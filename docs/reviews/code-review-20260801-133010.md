# Code Review

## Scope

- Mode: current changes
- Branch: `fix/holdings-tag-json-text`
- Base: `origin/main@63cce99`
- Output file: `docs/reviews/code-review-20260801-133010.md`
- Included scope: S1 changes in `src/app/holdings_validation.py`, validator and
  account-preflight regressions, the real raw-record -> validator -> workflow
  plan -> frozen snapshot -> NAV warning path, schema/writer/repository
  contracts, and implementation-plan alignment
- Excluded scope: unrelated user-owned
  `docs/reviews/code-review-20260801-084655.md`; release, deployment, live
  mutation, unrelated Holdings fields, and full-repository behavior
- Parallel review coverage: 无；the slice is one small validation path and was
  reviewed end to end by the primary reviewer

## Findings

未发现实质性问题。

The implementation keeps raw Feishu facts unchanged, strictly accepts only
JSON arrays of strings, preserves malformed raw evidence, leaves
`canonical_record_payload()` and case identity unchanged, and materializes
typed tags from the validated outcome rather than the transport string.

## Adversarial failure pass

- `"[]"` reaches `_optional_tag()`, decodes to an empty list, becomes
  `optional_missing`, is excluded from actionable workflow outcomes, and
  therefore produces neither a case nor `holdings attention` warning.
- `'["core", "income"]'` becomes a list-valued valid outcome and reaches
  `Holding.tag` without character splitting.
- Malformed JSON, JSON object/null/scalar values, mixed arrays, native dicts,
  and native mixed arrays all remain `TAG_INVALID` with the original raw value
  exposed for audit.
- Native-list behavior is preserved. Missing/blank digest canonicalization and
  native/text array digest equivalence remain unchanged because the digest path
  was not edited.
- Tag remains optional and nonblocking; required-field and duplicate-record
  behavior is outside the changed branch and untouched.

## Ownership and coupling review

- Validation owns interpretation of the raw field for preflight; the fix does
  not move normalization into the source repository or presentation layer.
- The writer and schema remain the transport source of truth.
- No new public API, dependency, codec abstraction, reverse import, shared
  mutable state, or cross-module special case was introduced.
- Workflow state consumes the existing `FieldOutcome` contract and requires no
  tag-specific branch.

## Validation reviewed

- Red phase: exactly three target regressions failed before implementation.
- Green focused gate: `168 passed in 1.05s`.
- `git diff --check`: pass.
- Tests cover the real preflight warning boundary and strict invalid-shape
  behavior rather than only directly invoking the parser.

## Open Questions

- 无。

## Residual Risk

- Full-suite and compile checks remain pending at this code-review point and
  are assigned to the approved validation gate before aggregate DeepReview.
- Existing production case convergence and warning removal require a future
  formal preflight after separately authorized release/upgrade; no direct state
  repair is part of this slice.
- A live modified-code run was not performed because it would require a
  deployment outside current authorization.

All residual risks are classified and none blocks S1 acceptance.

## Conclusion

`pass`; no fix is required. Next entry point: S1 re-review/pass evidence.
