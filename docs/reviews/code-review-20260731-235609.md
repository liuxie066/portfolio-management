# Code Review

## Scope

- Mode: current changes
- Branch or PR: `plan/holdings-validation-event-trigger`
- Base: `16ed0d5`
- Output file: `docs/reviews/code-review-20260731-235609.md`
- Included scope: S3 combined CLI, target preflight, runtime composition,
  callback fan-out, worker shutdown, installer unit compatibility, operator
  documentation, and focused tests
- Excluded scope: live Feishu SDK/subscription, installed systemd mutation,
  release, deployment, and any production data write
- Parallel review coverage: 无

## Findings

No material correctness, boundary, compatibility, or shutdown findings were
found in the reviewed S3 execution path.

## Evidence reviewed

- `events status` resolves and validates both targets, reads both inbox statuses,
  and uses only SDK availability/config evidence. It does not construct the
  adapter or claim remote subscription/connection health.
- `events subscribe` and `events listen` reject missing confirmation before
  backend construction.
- Target uniqueness validation runs before adapter creation in subscribe and
  before storage/state/worker construction in listen.
- The combined listener injects one operation store into both inboxes and the
  cash-flow completion service, so FX confirmation, event completion, and typed
  receipts use the same durable authority.
- One adapter callback fans the raw payload into both exact normalizers; tests
  prove holdings and cash-flow events have one accepting handler while unknown
  tables have none.
- Both workers share one stop event and every successfully started worker is
  joined in `finally`, including partial-start failure paths.
- The shared adapter already deduplicates identical Base file tokens and reports
  per-file subscription outcomes.
- The legacy unit name and holdings-only CLI remain intact; only the generated
  unit command/description change to combined runtime scope.
- Documentation preserves the separate FX and CASH holding-effect confirmation
  boundaries.

## Open Questions

- 无。

## Residual Risk

- Thread liveness is not independently supervised while the SDK call blocks;
  each worker catches cycle failures and systemd restarts the overall process on
  process exit. Cross-thread health telemetry remains outside this work unit.
- Live Base permission, subscription, callback delivery, and write/readback
  behavior require a separately authorized canary.
- A future installer apply writes the updated unit but activation/restart remains
  a separate explicit operational action.

## Validation

- Focused/regression pytest: `116 passed in 1.02s`
- Ruff: `All checks passed!`
- `git diff --check`: passed

## Conclusion

`pass`
