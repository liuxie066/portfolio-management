# Plan Re-Review

- Reviewed target: `docs/gateflow/repo-review-27/implementation-plan.md`
- Previous review: `docs/reviews/plan-review-20260719-170245.md`
- Work unit: `repo-review-27 correctness hardening`
- Scope: fixes for all six failed-plan findings plus full regression pass over architecture, state machines, sequencing, tests, overcoupling, and historical compatibility
- Conclusion: `pass`

## Finding status

### 01-已修复-[严重]-stale compensation cannot overwrite later writes
- Plan evidence: process lock order is `account -> task`; every target persists `before` and `target`; retry uses compare-and-set and fails with `state_conflict` when current matches neither.
- Validation requirement: later legitimate write, current==target crash replay, and mixed target states are explicit S2 tests.

### 02-已修复-[高]-one recovery task owns the complete partial financial unit
- Plan evidence: one task contains ordered narrow `targets[]`; normal write captures all before/target states before ledger creation, stops new side effects at first failure, and returns one task ID for the complete unresolved unit.
- Validation requirement: CASH/MMF split partial and holding-before-cash failure are explicit fault paths.

### 03-已修复-[高]-NAV snapshot recovery evidence has a durable discovery path
- Plan evidence: local task is durable before details patch; existing-NAV detection checks both persisted details and unresolved local tasks by related NAV record; resolution requires snapshot convergence and details completion before `RESOLVED`.
- Validation requirement: details patch failure and old row without marker but with task are explicit tests.

### 04-已修复-[高]-deadline path has no ownerless worker
- Plan evidence: valuation uses bounded native batch HTTP plus sequential remaining provider/FX calls; no outer worker, executor escape, or `shutdown(wait=False)` cancellation is allowed.
- Validation requirement: return-time and no-late-cache-write assertions cover timeout behavior.

### 05-已修复-[高]-Feishu type mapping is implementation-ready
- Plan evidence: official field schema mapping is fixed to 1 text, 2 number, 3 single select, 4 multi select, 5 date, with `ui_type` and documented alternatives handled explicitly. No blocking question remains.
- Validation requirement: typed multipage fixtures cover accepted alternatives and strict mismatch.

### 06-已修复-[中]-Futu legacy ETF and filtered funds have distinct reconciliation order
- Plan evidence: incoming eligible rows are joined first against all same-account/broker rows; only unmatched canonical stock/exchange-fund rows become zero candidates. Futu takes the same account lock.
- Validation requirement: legacy ETF update, unrelated fund untouched, and absent canonical stock zero are explicit tests.

## Adversarial state-machine pass

- Lock ordering has one direction and no multi-account lock path.
- Compensation crash after side effect is absorbed by current==target; stale current conflicts rather than overwrites.
- A NAV details patch failure cannot erase the local task or become completed-existing.
- Repair patch apply exposes partial prefix and deterministic resume/rollback.
- Feishu malformed chunk cannot update repository counts/caches as if complete.
- Write-capable service timeout remains outcome-unknown and cannot direct-fallback.

## Architecture boundary review

- Validation and economic-unit orchestration remain in `TradeService`.
- Compensation persists narrow storage targets and calls existing repository/service boundaries; it does not own trade semantics.
- Pricing deadline is passed through a small remaining-time contract, not by coupling valuation to provider internals.
- Feishu protocol validation remains in the transport client; repository cache mutations consume validated results.
- Loopback policy is enforced in the ASGI app while wrapper scripts only set explicit configuration.

## Best-practice / optimal-solution review

The revised plan uses the smallest credible correctness mechanisms: stdlib file locks, append-only events, compare-and-set targets, existing repositories, bounded HTTP timeouts, and middleware. SQLite, a generic saga engine, distributed locks, and an authentication platform remain correctly excluded.

## Overengineering review

No speculative abstraction remains. The only new generic-looking pieces are a small process-lock context manager and a fixed `targets[]` union, both directly required by multiple accepted findings.

## Overcoupling review

- Account lock coordination is shared only by product mutation entry points; low-level repositories are not promoted as public concurrency boundaries.
- Slices remain reviewable: S1 establishes locks/idempotency, S2 recovery semantics, S3 Futu/installer, S4 NAV, S5 pricing, S6 Feishu, S7 service safety.
- Cross-slice dependencies are explicit and sequenced.

## Historical-data compatibility

- Runtime replay metadata is not serialized.
- Existing financial rows require no migration.
- Legacy delta compensation tasks remain visible/manual-only.
- Existing NAV rows without failure evidence retain complete-existing behavior.
- Legacy ETF enum rows are matched only when an eligible incoming code exists.
- Live Feishu schema is inspected, not mutated.

## Open Questions

无。

## Residual Risks

- Cross-host Feishu uniqueness: assigned to later infrastructure decision and documented.
- Legacy delta compensation execution: explicit operator decision, no automatic mutation.
- Explicit unauthenticated remote mode: documented operator-controlled risk.

All residual risks are classified and non-blocking for the approved work unit.
