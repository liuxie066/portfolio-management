# Code Review

## Scope

- Mode: current changes
- Branch or PR: `fix/nav-finality-write-authority` — Slice S2 workspace changes
- Base: `14cb8c3` (`gateflow: accept nav-finality-write-authority S1`)
- Output file: `docs/reviews/code-review-20260721-123836.md`
- Included scope: repository mutation locking, all changed overwrite defaults, CLI argument mapping, publisher write-state machine, HTTP/client/application propagation, documentation, and focused regression tests
- Excluded scope: S1 finality implementation already accepted at `14cb8c3`; Yahoo/Finnhub/Futu provider behavior; cross-host Feishu uniqueness; deployment and production data mutation
- Parallel review coverage: 无；scope is one cohesive write-authority chain and was reviewed end-to-end locally

## Findings

未发现实质性问题。

The review traced these critical paths:

- `pm nav record` / `pm daily` -> service client or direct `PortfolioService` -> account/daily service -> `PortfolioManager.record_nav` -> `NavRecordService` -> storage facade -> repository.
- Publisher CLI -> `build_report_data` -> service/direct daily bundle, including default, explicit write, missing confirmation, conflicting flags, existing-row dry-run preview, and HTML publication independence.
- Repository public mutation methods -> one global key -> internal lock-free helpers -> remote Feishu write and local index invalidation.
- Repair and close paths -> explicit overwrite arguments -> newly safe repository defaults without loss of deliberate maintenance behavior.

The branch ordering and parameter-effectiveness checks passed:

- `overwrite_existing` now originates as false at public boundaries and reaches the repository unchanged.
- Bulk persistence is selected only when overwrite was explicitly enabled; non-overwriting calls stay on the single-row path that can refuse an existing row.
- Publisher default sends `dry_run=true`, `confirm=false`, and uses overwrite only for read-only preview; explicit writes send `dry_run=false`, `confirm=true`, `overwrite_existing=false`.
- `--dry-run` and `--write-nav` are mutually exclusive, so a legacy safety flag cannot be silently ignored in favor of a write.
- Public repository methods acquire the lock once and call only internal lock-free helpers, preserving the documented account-lock -> repository-lock ordering.

## Open Questions

- 无。

## Residual Risk

- Same-host serialization relies on all writers using the repository public mutation boundary and the same configured data directory. Direct external Feishu writers or another host can still race because no remote uniqueness constraint exists.
- Validation is local; no live Feishu mutation or multi-process production canary was run, by design for this Gateflow work unit.
