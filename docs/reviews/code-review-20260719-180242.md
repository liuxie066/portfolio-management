# Code Review — repo-review-27 S3

## Scope

- Mode: current changes
- Branch or PR: `gateflow/fix-repo-review-27`
- Base: `4446eb3`
- Output file: `docs/reviews/code-review-20260719-180242.md`
- Included scope: Futu position eligibility/diff/write path, account locking, installer source/target env parsing and merge behavior, and focused tests.
- Excluded scope: later approved S4-S7 findings, production/remote state, release/deployment execution.
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Evidence and adversarial pass

- The incoming `STOCK`/`ETF` eligibility boundary is evaluated before existing-row reconciliation.
- Existing legacy market-fund rows are matchable only to an incoming code; unmatched legacy fund rows are excluded from zero candidates and the empty-stock guard.
- Unmatched canonical stock/`EXCHANGE_FUND` rows remain eligible for explicit zero replacement.
- Duplicate normalized incoming positions and duplicate existing Futu business keys fail closed.
- Futu provider I/O occurs before the account lock; the authoritative diff read and all account writes occur inside one account lock, matching compensation lock ownership.
- Installer parses both source and target before any write; duplicate keys fail before directory/file mutation.
- Missing source files and missing/empty source values do not delete deployed receipt values.
- Non-managed target lines remain unchanged; explicit managed values are replaced in place and missing managed lines are appended.
- Digest evidence proves source-missing reruns preserve the target env byte-for-byte.

## Project instruction check

- Changes are limited to the approved S3 production files and focused tests.
- No new dependency or speculative configuration framework was introduced.
- No production access, credential change, release, deployment, merge, or live-data mutation occurred.

## Coupling and semantic ownership

- Futu security classification remains provider input; product-owned canonical zero eligibility remains in the sync service.
- The installer owns only the three receipt keys and preserves unrelated operator-owned env content.
- Account serialization reuses the shared process-lock contract rather than adding a second lock mechanism.

## Validation

- Focused tests: `37 passed`.
- Compile check: passed.
- Diff whitespace check: passed.

## Open Questions

无。

## Residual Risk

- Unsupported Futu security types remain `intentionally ignored and observable`; no automatic mutation is attempted for them.

## Decision

- Gate result: `code review pass`
- Accepted findings: none.
- Completion status: source findings 05 and 17 are fixed within the approved S3 contract.
