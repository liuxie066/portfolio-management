# Code Review

## Scope

- Mode: aggregate current changes
- Branch or PR: `fix/non-futu-cash-holdings-authority`
- Base: `main@0f496dac`
- Output file: `docs/reviews/code-review-20260814-110241.md`
- Included scope: complete Gateflow work unit from non-Futu holding observation
  through effect state, preview/confirm/correction, NAV blocker projection,
  structured refusal propagation, daily-job item preservation, receipt rendering,
  tests, CLI choices, and operational documentation
- Excluded scope: unrelated untracked `assets/` and
  `docs/reviews/code-review-20260801-084655.md`; live Feishu/OpenD reads or
  writes; release, deployment, and timer changes
- Auxiliary design coverage: Kimi's independent authority/conflict proposal was
  checked against the implemented effect state machine. Its double-counting
  concern is covered by the mandatory, preview-hash-bound `apply_delta` versus
  `already_reflected` declaration without adding a conflict table or state.

## Findings

未发现实质性问题。

The aggregate path is internally consistent:

- non-Futu direct CASH drift becomes a terminal `record_only` audit baseline
  and cannot block NAV, while Futu and compensation-owned drift retain their
  existing fail-closed owners;
- a non-Futu cash-flow effect remains pending until the operator explicitly
  chooses whether the fresh manual balance excludes or includes the event;
- `already_reflected` uses an ordinary CAS-checked no-op target and finishes
  `applied`, so a later correction uses only the delta from the previous applied
  version;
- account-specific blocker operations survive the typed
  `CashFlowDatasetRefusal` and account/job result chain; the receipt renderer
  uses those structured facts and never exposes the nested raw exception;
- generic daily-job failures, Futu-only effects, record-only history,
  compensation, and CLOSED NAV preserve their existing compatibility paths.

The solution reuses the existing effect kind, states, target/CAS machinery,
fingerprints, compensation, and result envelope. No schema, timer, new broker
abstraction, or inferred inclusion heuristic was introduced.

## Open Questions

- 无。

## Residual Risk

- The inclusion declaration is operator-owned. A correction touching multiple
  non-Futu identities uses one effect-wide declaration; mixed real-world state
  must be normalized before confirmation and every target row is visible in the
  preview.
- The NAV receipt expands the first blocker and counts the remainder; the full
  durable result and `pm cash-flow review --account ACCOUNT --json` retain all
  details.
- No live Feishu receipt, OpenD observation, production scan, or NAV write was
  performed.
- Repository-wide Ruff still reports 103 pre-existing issues outside this
  change. Ruff passes for every changed Python file; the full test suite and
  compile check pass.

## Validation Evidence

```text
PYTHONPYCACHEPREFIX=/tmp/pm_non_futu_cash_full python3.12 -m pytest -q -p no:cacheprovider
1458 passed in 8.95s

python3.12 -m ruff check <all changed Python files>
All checks passed!

PYTHONPYCACHEPREFIX=/tmp/pm_non_futu_cash_compile python3.12 -m compileall -q src scripts tests
pass

git diff --check main...HEAD
pass
```
