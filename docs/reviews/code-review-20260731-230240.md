# Code Review

## Scope

- Mode: aggregate fix re-review
- Branch or PR: `plan/holdings-validation-event-trigger`
- Base: accepted plan `9966b30`
- Prior review: `docs/reviews/code-review-20260731-230028.md`
- Included scope: complete S1-S4 implementation and `DR-AGG-01` fix
- Excluded scope: live external operations, listener activation, push/PR,
  release, deployment, and production mutation
- Parallel review coverage: none; the main reviewer re-walked event acceptance,
  provider evidence, workflow persistence, confirmation, and NAV consumption

## Findings

未发现实质性问题。

`DR-AGG-01` is closed: event-only planning now rejects any current provider
evidence error before constructing materializations. The existing worker error
transition marks the inbox row `failed_retryable`; the transaction contains no
case or receipt. A deterministic regression advances retry time, restores the
provider, and proves the same event then reaches `processed` with one case.
NAV's separately reviewed exact manual-confirmation outage path is unchanged.

The aggregate contracts remain coherent:

- event metadata is trigger-only; all validation fresh-reads exact Feishu rows;
- currency is instrument/type/evidence-derived and never defaulted to CNY;
- conflicts are durable and require an exact human decision;
- event ingestion never applies, resolves, recovers, or writes holdings;
- official NAV independently performs fresh global/account preflight and
  consumes the exact frozen holdings digest without a reread;
- formal workflow/state failure blocks, dry-run remains state-write-free, and
  existing eligible final NAV stays idempotently skipped.

## Verification

- Aggregate focused suite: `328 passed in 1.11s`.
- Full repository suite: `951 passed in 24.51s`.
- Ruff on aggregate-fix files: passed.
- Each slice's touched-file Ruff gate: passed before its accepted commit.
- Python compilation: passed.
- `git diff --check`: passed.

## Open Questions

- 无。

## Residual Risk

- Live Feishu/Futu payload, permissions, subscription, connection, and receipt
  rendering require a separately authorized activation/release canary.
- The repository-wide Ruff baseline has 141 pre-existing findings in legacy
  scripts/tests and re-export modules; it was not expanded into this work unit.
- Cross-host/external writers remain outside same-host locking; NAV preserves
  the source time and digest of the exact frozen facts it consumed.

## Verdict

`pass`
