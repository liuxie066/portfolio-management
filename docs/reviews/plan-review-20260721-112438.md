# Plan Re-review

## Scope

- Reviewed target: revised `docs/gateflow/nav-finality-write-authority/implementation-plan.md`
- Prior review: `docs/reviews/plan-review-20260721-110432.md`
- Work unit: `nav-finality-write-authority`
- Branch: `fix/nav-finality-write-authority`
- Base: `origin/main@81e82e2`
- Output file: `docs/reviews/plan-review-20260721-112438.md`

## Finding re-review

### PR-01-已修复-[高]-Finality eligibility incorrectly depended on daily-job provenance
- Final eligibility now depends on supported version, `status=final`, matching NAV date, and no recovery state.
- `writer` remains mandatory audit provenance but is not used as lifecycle truth.
- The plan adds an explicit final validated-repair test.
- Final status: `已修复`.

### PR-02-已修复-[高]-Snapshot completeness predicate was underspecified
- The state machine now gives recovery precedence for persisted failure markers or unresolved compensation tasks.
- Absence of explicit snapshot fields is documented as the current successful steady state; no migration to `snapshot_status=complete` is assumed.
- Required tests cover absent fields, failed fields, and unresolved local tasks.
- Final status: `已修复`.

### PR-03-已修复-[中]-Metadata propagation was overcoupled
- Four independent metadata parameters were replaced by one immutable internal `NavWriteContext` owned by `src/app/nav_finality.py`.
- Public HTTP/CLI requests cannot claim arbitrary finality.
- Trusted entry methods construct validated contexts, and one internal object is serialized by `NavRecordService`.
- Final status: `已修复`.

## Assumptions re-tested

- Validated repair can produce an honestly final row without impersonating daily-job: satisfied.
- Existing successful rows do not require a new snapshot-complete marker: satisfied.
- Invalid finality combinations are rejected at one value-object boundary: specified.
- Repository lock ordering remains acyclic: repair acquires account/journal locks before repository global NAV lock; repository code does not acquire account locks.
- Legacy rows fail closed and are not auto-promoted: specified and documented.

## Open Questions

- None.

## Residual risks

- Cross-host writers remain outside the same-host lock: `assigned to later infrastructure decision`.
- Existing production rows need explicit operator handling: `operator-owned remediation; documented in current work unit`.
- Provider-observed quote dates remain absent: `assigned to later pricing reliability work unit`.
- Lock validation must avoid timing-only tests: `fixed requirement in current S2 test design`.

## Final conclusion

`pass-with-risks`.

The plan is code-generation-ready. All prior findings are `已修复`, remaining risks are classified, and implementation may proceed with S1 only.
