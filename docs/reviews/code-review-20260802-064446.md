# Code Re-Review

## Scope

- Mode: complete S8 workspace re-review after accepted fixes
- Branch: `gateflow/feishu-bitable-contract-repair`
- Base: `6cbe4110b72f` (`gateflow: accept S7 complete NAV contracts`)
- Prior review: `docs/reviews/code-review-20260802-061448.md`
- Fix artifact:
  `docs/gateflow/feishu-bitable-contract-repair/s8-fix.md`
- Review artifact: `docs/reviews/code-review-20260802-064446.md`
- Included production scope: valuation projection, NAV calculation and final
  invariant, finality vocabulary/validation, daily versus gap flow evidence,
  CLOSED NAV, fresh FieldState-aware maintenance reads, explicit immutable NAV
  history input, restricted derived patches, patch/backfill planning, complete
  CAS, journal integrity, apply/resume/readback/rollback, and CLI status
- Included verification scope: all S8 source/tests, implementation/scope/fix
  artifacts, exact S8 suite, full repository suite, Ruff, compile, and diff
  checks
- Excluded: live Feishu/Futu reads or writes, live schema mutation, historical
  repair execution, merge, release, deployment, and the unrelated pre-existing
  untracked `docs/reviews/code-review-20260801-084655.md`
- Parallel review coverage: none; the complete state machine was reviewed as
  one end-to-end calculation and mutation contract

## Findings

No material findings.

The three high-severity findings in the prior review are closed:

- Derived maintenance preserves valuation finality and the valuation run id,
  while repair identity and stable dataset fingerprints have a separate
  maintenance provenance.
- Complete fresh target/dependency evidence is validated before calculation;
  missing base components, duplicate dates, invalid predecessors, and later
  dependency drift fail closed.
- Journal CAS and fresh readback compare complete maintenance state while
  apply/rollback write only the declared changed subset. A post-attempt
  readback failure is reported as partial-write-possible.

The re-review also verified that maintenance calculation no longer publishes
hypothetical candidates into shared cache authority. It passes the complete
fresh working history as an immutable, non-persisting `nav-repair` input to the
canonical NAV service.

## Open Questions

- None.

## Residual Risk

- Feishu exposes a clear operation rather than distinct physical Missing and
  Null restoration operations. The journal preserves the distinction, and
  fresh readback remains the deciding evidence.
- Same-host locks do not prevent an external Feishu editor or another host from
  writing after the final readback. Complete fresh CAS/readback detects
  observed drift but cannot provide a remote atomic transaction.
- Legacy rows without a trustworthy finality remain non-final after derived
  maintenance. Promotion to `nav-repair/final` requires separate authority.
- S9/S10 still own normalized replayable snapshots and exact-set snapshot write
  authority; S8 does not pre-claim that completion signal.
- `skill_api.py` retains 13 pre-existing whole-file E402/F541 lint findings.
  Its S8 diff is limited to two missing-value defaults and matching docs; the
  remaining changed S8 source/test set is Ruff-clean.

## Validation

- Exact S8 pytest suite: `89 passed in 0.92s`.
- Full repository pytest suite: `1297 passed in 7.94s`.
- Scoped Ruff: `All checks passed!`.
- Python compileall and `git diff --check`: passed.
- No live external request or business-data mutation was performed.

## Conclusion

`pass`
