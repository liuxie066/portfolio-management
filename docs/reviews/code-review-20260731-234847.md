# Code Review

## Scope

- Mode: current changes
- Branch or PR: `plan/holdings-validation-event-trigger`
- Base: `9b1ba3e`
- Output file: `docs/reviews/code-review-20260731-234847.md`
- Included scope: S2 exact-record completion policy, shared FX-confirmation
  authority, semantic cash-flow receipts, receipt outbox routing, finite retry
  exhaustion, atomic event/receipt persistence, locks, and focused tests
- Excluded scope: S3 combined CLI/runtime wiring, live Feishu subscription,
  release, deployment, and cash-holding effect application
- Parallel review coverage: 无

## Findings

No material correctness, authority, durability, or regression findings were
found in the reviewed S2 execution path.

## Evidence reviewed

- `CashFlowEventCompletionService.process_record()` uses exact-record repository
  previews, repeats the preview inside `cash-flow-record-write:<record_id>`, and
  requires a fresh zero-change readback after apply.
- `evaluate_cash_flow_fx_confirmation()` is the single matching function for
  both the listener decision and NAV preflight. It fails closed on absent,
  malformed, stale, or unauthorized evidence.
- Invalid rows and foreign evidence gaps return terminal attention outcomes
  without reaching the repository apply call.
- `complete_cash_flow_event()` commits semantic receipts and the processed event
  outcome in one SQLite transaction.
- `mark_cash_flow_event_failed()` schedules attempts 1–3 after 1/5/15 minutes and
  converts attempt 4 to an atomic attention outcome plus typed receipt.
- Receipt keys and frozen payloads use normalized manual inputs and stable FX
  identity, excluding trigger/time/generated-field noise.
- The receipt dispatcher retains holdings routing and adds the cash-flow type
  without changing delivery unknown/retry semantics.

## Open Questions

- 无。

## Residual Risk

- S3 must inject the same operation-state store into the inbox and completion
  service and must always configure the terminal receipt factory.
- The current same-host lock cannot exclude another machine or a direct Feishu
  edit; repeat preview/readback reduces this risk but cannot make the remote
  write and local event transaction atomic.
- No live SDK or Feishu write canary was run because it is outside the confirmed
  local implementation boundary.

## Validation

- Focused/regression pytest: `61 passed in 1.09s`
- Ruff: `All checks passed!`
- `git diff --check`: passed

## Conclusion

`pass`
