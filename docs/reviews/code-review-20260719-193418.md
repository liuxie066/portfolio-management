# Aggregate DeepReview — repo-review-27 correctness hardening

## Scope

- Mode: current changes
- Branch: `gateflow/fix-repo-review-27`
- Base: `origin/main@d04c62496bb9682a5a3ac00b38efb34ad6f3e9b7`
- Reviewed head: `92b0f6f`
- Output file: `docs/reviews/code-review-20260719-193418.md`
- Included scope: the complete eight-commit Gateflow branch, all 27 source findings, seven implementation slices, slice review/fix artifacts, production code, tests, schema/runbook/service docs, and cross-slice state/lock/error interactions.
- Excluded scope: release/deployment/merge/live data mutation, production historical repair execution, new cross-host infrastructure, new authentication product, and unrelated observations outside the 27 approved findings.
- Parallel review coverage: prior specialized repository review artifacts supplied the source finding set; this aggregate pass independently checked their final branch integration and current validation evidence.

## Findings

未发现实质性问题。

## Source Finding Closure

| Finding | Final status | Owning slice | Final evidence |
|---|---|---|---|
| 01 idempotent replay still mutates holdings/cash | 已修复 | S1 | replay metadata returns persisted ledger result before target mutation; transaction/cash-flow regressions pass |
| 02 sell permits oversell/missing holding | 已修复 | S1 | authoritative pre-ledger holding/quantity validation under account lock |
| 03 invalid zero/negative/non-finite writes | 已修复 | S1 | shared write guard is invoked by trade/cash entry boundaries and compatibility cash writes |
| 04 secondary write failure reports success/no recovery loop | 已修复 | S2 | `PartialWriteError`, durable target task, compare-and-set retry, truthful partial state |
| 05 filtered Futu fund rows are zeroed | 已修复 | S3 | canonical stock/ETF zeroing boundary preserves unmatched fund rows |
| 06 persisted NAV partial becomes ordinary failure/skip | 已修复 | S4 | partial/recovery-required state propagates through account and daily-job layers |
| 07 foreign raw price treated as CNY | 已修复 | S5 | foreign valuation requires positive finite `cny_price` |
| 08 missing CNY quote becomes unit price | 已修复 | S5 | unit fallback is limited to CNY cash/MMF; securities remain unvalued |
| 09 timeout/cache-only still performs network work | 已修复 | S5 | single absolute deadline and cache-only selection before fixed/provider/FX paths |
| 10 US session uses Beijing weekday | 已修复 | S5 | New York-local session and DST-aware elapsed-time calculations |
| 11 zero/negative/non-finite quote accepted | 已修复 | S5 | central positive-finite quote/FX validation |
| 12 Feishu batch response cardinality/mapping unchecked | 已修复 | S6 | strict cardinality, record-ID mapping, and structured confirmed-prefix errors |
| 13 delete failure swallowed/cache cleared | 已修复 | S6 | delete failures propagate and cache invalidation follows confirmed success |
| 14 Feishu core HTTP lacks bounded timeout | 已修复 | S6 | configured/clamped connect/read tuple reaches token and data requests |
| 15 service timeout replays write through direct fallback | 已修复 | S7 | POST transport ambiguity is distinct; all CLI call sites explicitly classify fallback safety |
| 16 direct ASGI startup bypasses loopback restriction | 已修复 | S7 | application middleware checks actual client IP and defaults fail closed |
| 17 installer rerun deletes deployed receipt credentials | 已修复 | S3 | merge-only explicit replacement preserves target and unrelated env content |
| 18 NAV patch may silently miss targets/leave prefix | 已修复 | S4 | exact preflight, stable journal, partial result, resume, and rollback |
| 19 NAV repair misses first successor validation | 已修复 | S4 | default validation covers changed dates plus first chronological successors |
| 20 snapshot quantity rounded as money | 已修复 | S5 | independent eight-decimal quantity precision |
| 21 incomplete FX cache becomes fresh memory cache | 已修复 | S5 | both USD/HKD rates must be complete and positive finite before freshness update |
| 22 supported market suffixes not normalized consistently | 已修复 | S5 | one suffix canonicalization boundary preserves explicit market facts for routing |
| 23 transaction/cash-flow idempotency is check-then-create | 已修复 | S1 | same-host process lock encloses dedup check/create and failures fail closed |
| 24 compensation JSONL concurrent write loses tasks | 已修复 | S2 | append-only `O_APPEND` + process lock + flush/fsync event log |
| 25 NAV dry-run overwrites formal local snapshot | 已修复 | S4 | dry-run performs preview persistence only and skips formal local snapshot write |
| 26 foreign MMF fixed value ignores FX | 已修复 | S5 | USD/HKD MMF uses fixed cash FX conversion |
| 27 live schema check ignores type/pagination | 已修复 | S6 | full pagination and strict documented type-family comparison |

## Slice Review Finding Closure

| Review finding | Final status | Evidence |
|---|---|---|
| S2-01 NAV snapshot compensation projection omitted `details` | 已修复 | NAV index projection includes details; CAS recovery regression passes |
| S4-01 overlapping same-account repair journals | 已修复 | account lock wraps journal lock for apply/resume/rollback |
| S4-02 torn final JSONL event makes recovery unreadable | 已修复 | only malformed non-newline tail is recoverable/truncated; middle corruption fails closed |
| S4-03 recovery receipt displayed unknown error | 已修复 | canonical error propagates to receipt with recovery metadata |
| S5-01 canonicalization discarded explicit market facts | 已修复 | original suffix derives authoritative market/type before canonical provider key routing |
| S7-01 fallback helper had unsafe default | 已修复 | keyword is mandatory; AST check proves all 12 calls explicit (4 writes false, 8 reads true) |

## Adversarial Failure Pass

- Financial writes: duplicate request/content replay, oversell, missing holding, invalid numerics, ledger-success/target-failure, compensation persistence failure, target state conflict, crash before/after mutation, and concurrent retry all fail or converge without claiming false complete success.
- Lock/state integration: trade, Futu sync, compensation retry, and NAV repair use the shared account lock; nested lock order is account -> task/journal -> append log, with no reviewed reverse acquisition held concurrently.
- NAV: persisted partials remain visible, complete existing rows are absorbing, dry-runs avoid formal snapshots, repair preflight blocks ambiguous records, torn-tail recovery is narrow, and rollback is reverse-order compare-and-set.
- Pricing: foreign/raw/invalid quotes never enter CNY totals, cache-only misses perform no network/FX work, deadlines bound all owned retries/sleeps/requests, explicit suffix facts survive canonicalization, and no worker can outlive return.
- Feishu: malformed or partial batch responses expose only confirmed complete prefixes, compatibility fallback cannot replay a confirmed prefix, delete/cache/count transitions follow confirmed results, and schema pagination/type drift fails closed.
- Service boundary: read unavailability falls back once; write ambiguity and write-command unavailability fall back zero times; official daily publication also propagates outcome unknown; direct ASGI remote clients receive 403 despite spoofed headers.
- Installer/Futu: missing or partial source env preserves deployed receipt keys; only eligible Futu stock/ETF rows are replaced/zeroed and account writes are serialized.

## Project Instructions Pass

- No release, deployment, merge, PR-ready transition, reviewer request, live Feishu/Futu/portfolio mutation, credential change, or production repair execution occurred.
- Branch and commit ownership remained isolated; no unrelated dirty work was staged or reverted.
- The implementation uses stdlib locking/JSONL, existing service/repository boundaries, and current dependencies; no speculative workflow engine, distributed transaction system, authentication product, or storage replacement was introduced.
- Operator-visible contract changes are documented in schema, migration, runbook, service, and installer guidance; no version/changelog bump is included.

## Overcoupling and Semantic Ownership Drift

- Input validation and economic target planning remain at authoritative write services; repositories own atomic same-host dedup/storage facts rather than business workflows.
- Compensation owns durable recovery state and compare-and-set retry; Feishu remains a non-authoritative optional mirror for compensation tasks.
- NAV patch journaling owns repair state; normal NAV recording does not gain repair orchestration.
- Pricing canonicalization/validation/deadline ownership is centralized at pricing boundaries; providers consume authoritative hints and bounded I/O rather than redefining market semantics.
- Feishu protocol validation remains in `FeishuClient`; repositories update counts/caches only from normalized confirmed results.
- Service transport ambiguity, CLI fallback policy, ASGI client admission, and launcher bind validation remain distinct owners with explicit contracts.

## Validation

- Aggregate full repository suite after all accepted slice commits -> `657 passed in 3.34s`.
- `python3 -m compileall -q src scripts skill_api.py` -> passed.
- `git diff --check origin/main...HEAD` -> passed.
- Branch state -> clean; eight intended accepted commits ahead of `origin/main`.
- S7 structural fallback classification -> 12/12 call sites explicit; 4 write commands false, 8 read commands true.
- Official daily-report adversarial probe -> `PortfolioServiceOutcomeUnknown` propagated; direct fallback call count zero.
- Existing slice validations and re-reviews remain referenced by `docs/gateflow/repo-review-27/S1-implementation.md` through `S7-implementation.md`.

## Open Questions

无。

## Residual Risk

- Same-host process locks do not provide cross-host uniqueness/serialization; owner: `later infrastructure decision`.
- Historical unsupported delta compensation tasks remain inspectable but require operator-managed repair; owner: `operator runbook`.
- Existing production duplicates, partial writes, stale caches, or rounded snapshots are not automatically rewritten; owner: `separate explicit production-data work unit`.
- Unsupported Futu security types remain intentionally ignored and observable; owner: `future product support decision`.
- Exchange holiday completeness and historical suffixed cache-key migration remain outside S5; owner: `future pricing/calendar work if needed`.
- External market/Feishu protocols are validated with deterministic failure injection rather than live canary; owner: `later deployment/canary work unit`.
- Explicit remote service mode is unauthenticated; owner: `operator`, documented requirement for an authenticated outer boundary.
- Ambiguous service write outcome inspection remains operator-driven; owner: `later status-query work unit if operational evidence requires it`.

No residual risk is unclassified.

## Decision

- Gate result: `aggregate deepreview pass`.
- Source findings: `27/27 已修复`.
- Slice review findings: `6/6 已修复`.
- New aggregate findings: `0`.
- Open questions: none.
- Residual risks: all classified with owner/destination.
- Next gate: `accepted deepreview commit`.
