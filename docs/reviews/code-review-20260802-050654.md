# Code Review

## Scope

- Mode: current changes, final S7 aggregate re-review
- Branch or PR: `gateflow/feishu-bitable-contract-repair`, corrected S7 workspace diff
- Base: `433f04a`
- Output file: `docs/reviews/code-review-20260802-050654.md`
- Included scope: complete NAV registry projection and cache replay, remote/disk identity integrity, scoped/global duplicate audits, write-before-read decisions, update/create confirmed-failed-unknown receipts, cache fault recovery, consumer non-cash/regional semantics, scope-corrected legacy fixtures, and all S7 tests
- Excluded scope: accepted S1-S6 commits; S8 NAV formula/repair/backfill/CLOSED work; S9-S12; live Feishu/Futu/schema operations; merge/release/deploy; unrelated untracked `docs/reviews/code-review-20260801-084655.md`
- Parallel review coverage: 无；按当前单 Agent 边界完成最终主链路、故障路径和测试面复核

## Findings

未发现实质性问题。

The final diff closes every accepted S7 finding:

- complete canonical NAV fields survive fresh, memory, and versioned disk reads;
- malformed date/account/record identity fails closed without partial cache publication or write planning;
- scoped and global duplicate audits share canonical validation;
- confirmed, failed, and unknown write scopes are mutually exclusive and retain business target identity;
- deterministic schema rejection and transport ambiguity produce different receipts;
- post-fault cache authority is rebuilt only from a fresh complete read;
- persisted `stock_value` remains non-cash inclusive of its `fund_value` subset, while runtime equity/fund categories stay disjoint; and
- regional compatibility values remain exposure totals that may include classified funds/ETFs.

The `tests/test_feishu_storage.py` scope correction changes only normal-response
fixtures to include the account field projected by the real request. Dedicated
negative tests retain missing/mismatched identity coverage, so the correction
does not weaken the production contract.

## Open Questions

- 无。

## Residual Risk

- Feishu update/create has no cross-endpoint transaction. Confirmed prefixes are observable through structured scopes and fresh readback but cannot be automatically rolled back.
- Versioned disk cache is a complete replay source, not a distributed snapshot; external edits after the cache was published require an explicit fresh preload or an official write path.
- Historical NAV formula, daily/gap cash-flow basis, repair/backfill base immutability, and CLOSED invariants remain intentionally assigned to S8.
- Validation used fake clients and local caches only; no live tenant canary was authorized in this work unit.
