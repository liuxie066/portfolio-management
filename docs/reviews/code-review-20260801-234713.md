# Code Review

## Scope

- Mode: current changes
- Branch or PR: `gateflow/feishu-bitable-contract-repair`
- Base: `e45a30ec8934`
- Output file: `docs/reviews/code-review-20260801-234713.md`
- Included scope: S1 implementation plus fixes for DR-S1-01 through DR-S1-03
- Excluded scope: unrelated pre-existing review artifact; S2-S12; live Feishu operations
- Parallel review coverage: 无；重点复核 malformed response、registry projection、storage conversion和新增 regression assertions

## Findings

### DR-S1-RR-01-未修复-高-code 的宽松 int 强转仍允许 false 和小数伪装成成功码
- **入口/函数**: `FeishuClient._request()`
- **文件(行号)**: `src/feishu_client.py:203-223`
- **输入场景**: HTTP 200 JSON 包含 `{"code": false, "data": {}}`、`{"code": 0.5, "data": {}}` 或字符串 `"0"`。
- **实际分支**: `int(False)` 和 `int(0.5)` 都得到 0；字符串 `"0"` 也被接受。随后代码进入成功分支并返回 data。
- **预期行为**: 飞书协议的 `code` 必须是 JSON integer（且 bool 不能作为 integer）；任何类型漂移都应作为 malformed protocol response fail closed。
- **实际行为**: 第一轮修复阻止了缺失/null code，但仍通过宽松强转把部分非法值规范化为成功。
- **直接证据**: 第 207 行无类型约束直接执行 `int(data['code'])`；新增 malformed tests 覆盖 null，却未覆盖 bool、float 和 numeric string。
- **影响**: 与 DR-S1-01 相同，畸形响应仍可被读成空数据或写成功，只是触发集合变窄。
- **建议改法和验证点**: 明确要求 `type(raw_code) is int`，不要用 coercion；为 `false`、`0.5`、`"0"` 添加回归并确认均在调用方解释 data 前抛错。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

## Open Questions

- 无。

## Residual Risk

- S2 才负责 holdings 完整 business key、clearability 与 repository mutation planning；S1 client 仍保留兼容的最小 create row-required 集合。
- live schema comparison 尚未在本 slice 重跑；生产使用前仍需按计划执行 read-only external protocol gate。
