# Code Review — repo-review-27 S1

## Scope

- Mode: current changes
- Work unit: `repo-review-27 correctness hardening`
- Slice: `S1-financial-invariants`
- Selected base: `ce24f86`
- Reviewed target: S1 production changes, focused tests, implementation artifact, and the authoritative trade/cash/holdings call paths.
- Output file: `docs/reviews/code-review-20260719-172502.md`

## Findings

未发现实质性问题。

## Evidence and adversarial pass

- `TradeService.buy/sell/deposit/withdraw` rejects zero, negative, NaN, and infinity before account lookup, ledger creation, or cash/holding mutation.
- Lock order is account write lock first and repository idempotency lock second; no reviewed path acquires them in reverse order.
- Transaction content dedup is computed before random fallback request ID assignment, preserving stable default replay identity.
- Repository request-ID lookup now fails closed on transport/protocol errors rather than creating after an unknown lookup result.
- Replay returns the persisted transaction/cash-flow model, marks it with runtime-only `PrivateAttr`, and exits before economic side effects. A conflicting retry payload therefore cannot become the reported result.
- SELL resolves the exact account/broker holding and rejects missing, zero, or insufficient quantity before `add_transaction`.
- `update_holding_quantity` rejects missing holdings and negative resulting quantities instead of silently succeeding.
- `PortfolioSkill.add_cash/sub_cash` share finite positive validation and the account lock used by the authoritative service path.
- Cross-process evidence exercises `fcntl.flock`; repository/service tests separately prove one create on replay and zero repeated side effects.
- Serialization tests prove replay metadata is absent from model dumps and therefore from Feishu payload generation.

## Project instruction check

- Changes are limited to the approved S1 files and tests.
- No production access, credential change, release, deployment, merge, or live-data mutation occurred.
- Standard-library `fcntl` is used; no dependency or speculative abstraction was introduced.

## Coupling and semantic ownership

- Account-level economic serialization is owned by `TradeService` and compatibility cash entry points.
- Per-idempotency-key check/create serialization remains owned by Feishu repositories.
- Runtime replay state remains model-local and non-persistent.
- No ownership drift or circular dependency was found in the reviewed paths.

## Validation

- Focused tests: `173 passed`.
- Compile check: passed.
- Diff whitespace check: passed.

## Open Questions

无。

## Residual Risk

- Cross-host duplicate creation remains `assigned to later infrastructure decision`; local `fcntl` locking deliberately provides only a same-host guarantee.
- Crash-safe compensation for secondary write failures is `covered by later approved slice S2`.

## Decision

- Gate result: `code review pass`
- Accepted findings: none
- Completion status: `已修复` for S1 source findings 01, 02, 03, and 23 within the approved same-host contract.
