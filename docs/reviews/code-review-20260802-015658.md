# Aggregate Code Re-Review

## Scope

- Mode: branch aggregate re-review
- Branch: `codex/feishu-dual-app-credentials`
- Base: `origin/main@59625b0d6c666da338c0a520e85a221932846949`
- Prior review: `docs/reviews/code-review-20260802-015353.md`
- Fix artifact:
  `docs/gateflow/feishu-dual-app-credentials/fix-aggregate-review.md`
- Output file: `docs/reviews/code-review-20260802-015658.md`
- Included scope: aggregate fix plus the complete S1-S4 credential resolver,
  consumers, status/preflight, systemd/installer, runbooks, and tests
- Excluded scope: live credentials, tenant permissions, Feishu API calls,
  subscription, service activation, release, and deployment
- Parallel review coverage: 无；the branch was re-reviewed as one end-to-end
  credential delivery and authorization boundary

## Findings

未发现新的实质性问题。

## Prior finding status

### AGG-CR-01 — 已修复

Every installed credential-bearing unit keeps its exact least-privilege grants
and wraps every business `ExecStart` in `/usr/bin/env`. The wrapper overwrites
both secure mode and `CREDENTIALS_DIRECTORY` with the exact system-service path
after the shared environment is assembled. Both preflight commands use the
same wrapper. The transient Base subscription command uses its own exact unit
path and only the Bitable credential.

This ordering matches the official `systemd.exec` precedence contract: although
`EnvironmentFile=` overrides unit `Environment=` and service-manager variables,
the exec wrapper's assignments are applied by `/usr/bin/env` to the final child
process. The documented system-service credential path follows systemd's
supported `/run/credentials/UNITNAME` form.

## Evidence

- Installer/documentation suite: `32 passed`.
- Full suite: `1103 passed`.
- Python compile, `bash -n scripts/install.sh`, and `git diff --check`: passed.
- Hostile-env regression preserves both conflicting shared env assignments and
  proves every service `ExecStart`, including both preflight commands, has the
  authoritative exact-unit wrapper.
- Manual unit-grant trace: Bitable-only and Conversation-only units remain
  isolated; dual-role units receive exactly two credential names.

## Open Questions

- 无。

## Residual Risk

- Actual Linux unit parsing/decryption and tenant-side Feishu behavior remain
  separately authorized deployment checks.
- Existing plaintext shadow cleanup remains manual and separately authorized.

## Conclusion

`pass`

The aggregate finding is fixed. The branch is acceptable for source delivery.
