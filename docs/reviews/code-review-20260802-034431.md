# Code Review

## Scope

- Mode: second S5 re-review after `DR-S5-RR1`
- Work unit: `feishu-bitable-contract-repair` / S5
- Base: `8c9266e`
- Prior reviews: `docs/reviews/code-review-20260802-033515.md`, `docs/reviews/code-review-20260802-034145.md`
- Included: final S5 diff plus compatibility methods remaining on the cash-flow repository facade
- Validation observed: S5 `119 passed`; previous full suite `1230 passed`

## Findings

### 1-未修复-中-遗留 FX resolver 仍允许仅凭 cny_amount 反推外币汇率，绕开 canonical date/source 证据定义

- **入口/函数**: `FeishuStorage._resolve_cash_flow_exchange_rate()` → `CashFlowRepository._resolve_cash_flow_exchange_rate()`
- **文件(行号)**: `src/feishu/_cash_flow_mixin.py:114-128`; `src/feishu/repositories/cash_flow_repository.py:1070-1092`
- **输入场景**: 任一旧调用方传 `currency=USD, amount=10, cny_amount=72, rate_cache={}`，或传一个没有 date/source 的裸 rate cache。
- **实际分支**: helper 直接返回 `72/10=7.2`；若没有 cny_amount，则也接受裸 `USDCNY` rate。它不要求 flow_date、rate_date 或 traceable source，也不复用本切片的新 evidence contract。
- **预期行为**: 外币 rate 只能在带 flow_date、同日证据和 source 的 reconcile plan 中解析；没有这些参数的遗留 helper 应 fail closed，不能成为第二套计算权威。CNY identity rate 1 可安全保留。
- **实际行为**: repository 内仍存在一条与 canonical rule 冲突的可调用路径，后续维护者或外部兼容调用可重新引入“有 cny_amount 就等于有 FX evidence”的旧语义。
- **直接证据**: 全仓 `rg` 显示该 helper 只由 compatibility facade 暴露、没有内部生产调用，因此可在不影响当前主路径的情况下封锁外币分支。
- **建议改法和验证点**: CNY 返回 1；所有外币调用抛出稳定错误并指向 `reconcile_cash_flows()`，不再从 amount/cny_amount/rate_cache 推导。新增兼容回归证明外币 helper 零写入且 fail closed。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

- 无。

## Residual Risk

- 旧 facade 方法暂时保留以提供明确 fail-closed 错误；彻底删除需单独处理潜在外部 Python 调用兼容性。
