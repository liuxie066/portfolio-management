# Code Review

## Scope

- Mode: current changes re-review after fixes
- Branch or PR: `fix/receipt-action-contracts`
- Base: `main@ffdeece`
- Output file: `docs/reviews/code-review-20260804-142500.md`
- Included scope: complete current receipt-action work unit, including Cash Flow receipt production/rendering/dispatch, durable outbox terminal-state behavior, compensation evidence, Futu sync failure evidence/rendering, action commands, documentation, and all changed or new tests.
- Excluded scope: pre-existing unrelated untracked `assets/` and `docs/reviews/code-review-20260801-084655.md`; the prior review artifact `docs/reviews/code-review-20260804-140832.md` except as finding input; live Feishu delivery, Futu OpenD calls, production data, release, and deployment.
- Parallel review coverage: 无；本次由主 reviewer 独立沿 `cash-flow effects scan/confirm -> durable outbox -> renderer -> Feishu` 和 `sync_futu_holdings -> sync_portfolio -> cash_mmf -> renderer -> Feishu` 两条真实入口重新走读，并复核上一轮两个确定性反例。

## Prior Finding Verification

### DR-01 — 已修复

- 未知 `receipt_type` 现在由显式 contract exception 分类为 `retryable=false`，不会创建 Feishu client 或发起 provider 请求（`src/app/cash_flow_effect_receipt_service.py:104-115,157-164`）。
- dispatcher 将该结果持久化为 `invalid`，追加单次 `receipt_invalid` event，并在返回 envelope 中暴露 `invalid` 计数；网络、provider 或配置失败仍维持 `failed`/retryable 语义（`src/app/cash_flow_effect_receipt_service.py:58-102`; `src/app/cash_flow_effect_store.py:962-989`）。
- pending 查询只选择 `pending`/`failed`，而 terminal invalid evidence 可通过独立查询读取，因此 invalid rows 不再占用下一轮 limit（`src/app/cash_flow_effect_store.py:927-960`）。
- 回归测试构造 100 条未知类型，证明首轮全部终止为 invalid、没有 provider call、只追加一次 invalid event；随后插入的已知 `runtime_error` 下一轮正常发送，第三轮不再尝试 poison rows（`tests/test_cash_flow_effect_receipt_service.py:134-209`）。

### DR-02 — 已修复

- MMF 计数现在只有在 nested `cash_mmf.success=true` 且 `created`、`updated` 两个字段均存在时才展示；失败或字段不完整统一显示“MMF 结果未确认”，不会从缺失字段推导零（`src/app/futu_sync_receipt_service.py:123-139`）。
- Futu producer 的成功结构确实同时提供 success 与两个计数字段；失败结构只保留真实 error/stages/partial-write evidence，因此 renderer 条件与 producer contract 对齐（`src/app/futu_balance_sync_service.py:839-905`）。
- golden regression 同时覆盖 partial-write failure 不出现伪零、明确成功零变更仍显示 0、以及 success 但计数字段缺失时保持未确认（`tests/test_futu_sync_receipt_service.py:135-207`）。

## Findings

未发现新的实质性问题。上一轮 DR-01（高）和 DR-02（中）的失败分支均已收敛，修复没有改变 Cash Flow effect 的业务最终状态，也没有把未确认的 Futu 写入结果包装成成功事实。

## Open Questions

- 无。

## Residual Risk

- 本次没有调用真实 Feishu、Futu OpenD 或生产 timer；provider 渲染、真实消息 ID 和自然运行回执仍需在后续被单独授权的发布/升级后验证。
- transport/config/provider failure 仍按既有 contract 保持可重试 `failed`；本次只把确定性的未知 receipt contract failure 从该队列中隔离，不改变既有重试策略。
- `invalid` evidence 当前通过 store API/SQLite 审计读取，本 work unit 没有新增独立 CLI 列表入口。

## Validation

- Changed-file Ruff: passed.
- Focused receipt/store regressions: `19 passed`.
- Related Cash Flow/Futu/NAV/service combination: `205 passed`.
- Full repository suite: `1423 passed in 16.20s`.
- `git diff --check`: passed.
