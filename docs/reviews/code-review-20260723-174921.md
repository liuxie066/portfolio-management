# Code Review

## Scope

- Mode: current changes
- Branch: `fix/pm-feishu-receipt-title`
- Base: `origin/main@ce5cfb4`
- Output file: `docs/reviews/code-review-20260723-174921.md`
- Included scope: accepted goal/plan, `FeishuClient.send_post_message`,
  receipt-shell validation and conversion, serialized Feishu post request,
  Futu and NAV service migration, dry-run/config/error/result invariants, and
  changed-path tests
- Excluded scope: unrelated Feishu table APIs and text callers, Futu sync
  business logic, NAV valuation/persistence/finality, live Feishu rendering,
  release, deployment, and production mutation
- Parallel review coverage:
  - Protocol/parser reviewer: `src/feishu_client.py`,
    `tests/test_feishu_client.py`, `_request()` boundary, malformed input,
    payload shape, H1/H2/spacing/Unicode, return contract, and test truthfulness
  - Service/state reviewer: both receipt services, their tests, and application
    parent-result isolation
  - Main reviewer: complete diff, accepted plan alignment, ownership,
    overcoupling, residual-risk classification, and combined call paths
  - Uncovered: live desktop/mobile Feishu rendering

## Findings

未发现实质性问题。

## Evidence reviewed

- `send_post_message()` validates the receiver and structural receipt shell
  before token or HTTP work.
- The first H1 becomes `zh_cn.title` and is absent from the body.
- Exact H2 lines become bold text nodes without hash markers.
- Ordinary Unicode lines stay ordered plain-text paragraphs; internal blank
  runs become one non-visible spacer paragraph.
- The request uses the existing endpoint and receive-id type, changes only
  `msg_type` and serialized content shape, and reuses `_request()` error
  handling.
- Both production services now call only `send_post_message()` while retaining
  their existing dry-run, missing-config, exception, success, and `message_id`
  branches.
- Test fakes expose only the new method, so a regression to the text sender
  fails directly.
- The focused suite passed: `87 passed in 0.34s`.
- The protocol/parser reviewer independently ran `49 passed`.
- The service/state reviewer independently ran `87 passed in 0.32s`.

## Open Questions

- 无。

## Residual Risk

- Feishu desktop/mobile interpretation of the non-breaking-space paragraph:
  `assigned to an operator-owned live canary after an authorized release; no
  live send is allowed in this work unit`.
- Futu's unchanged missing-config branch lacks a service-local direct test, but
  the equivalent NAV branch is covered and the Futu branch was not modified:
  `low-risk pre-existing coverage gap; not a blocking S1 finding`.
- A `code=0` response without `message_id` remains reported as success with a
  null ID, matching the pre-existing text-sender contract:
  `pre-existing client-wide response contract; assigned to a separate Feishu
  client-hardening work unit if desired`.
