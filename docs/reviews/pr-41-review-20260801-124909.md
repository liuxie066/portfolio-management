# Code Review

## Scope

- Mode: PR re-review after synchronizing current `main`
- Branch or PR: [#41 Fix holdings case precondition identity](https://github.com/liuxie066/portfolio-management/pull/41)
- Author: `liuxie066`
- Reviewed source head: `codex/fix-holdings-case-precondition-identity@cacaf82b3ecafa7932c3d35c8a23a1885217c875`
- Base: `main@b36c908faff25ebc19b5d6586e75581d9c3947de`
- Output file: `docs/reviews/pr-41-review-20260801-124909.md`
- Included scope: complete PR diff against current main, main-sync conflict
  resolution, field-specific precondition identity, legacy migration,
  receipt-suppression integration, NAV/event/manual paths, tests, PR metadata,
  and GitHub checks
- Excluded scope: live Feishu or production database mutation, merge into main,
  release, remote upgrade, approval, reviewer requests, and unrelated local
  untracked `docs/reviews/code-review-20260801-084655.md`
- Parallel review coverage: none; this re-review targets the cohesive PR diff
  and the two manually resolved test conflicts

## Findings

未发现实质性问题。

GitHub reported the earlier PR head as conflicting after `main` advanced to
include the Bitable subscription correction and daily-NAV holdings receipt
aggregation. Synchronizing that base produced two textual conflicts, both in
tests. The resolution retains both required imports and both independent test
sets: the legacy precondition migration regressions from this PR and the
receipt-suppression/lifecycle aggregation regressions from current main. No
production-code conflict required manual selection.

The resulting PR diff against current main still preserves the stable
`holdings-case.v1` case key and restricts the new versioned precondition to the
field being acted on. `currency` and `asset_class` continue to depend on raw
`asset_type`; unrelated fields do not. Legacy migration remains limited to
recognized unprefixed digests, same semantic payloads, eligible states, and a
valid keep-current scope. Unknown contracts, changed relevant dependencies,
stale scopes, and in-flight states remain fail-closed.

The current main receipt-suppression path does not weaken migration auditing:
precondition migration still emits exactly one durable case event inside the
materialization transaction while enqueueing no migration receipt. The merged
tests confirm both 32-case receipt suppression and legacy migration lifecycle
events, and the full suite confirms the integrated NAV and listener behavior.

## Open Questions

- None.

## Residual Risk

- **Later authorized release/upgrade work unit:** once v2 state is persisted,
  the preceding binary cannot consume it. Preserve the accepted suspended
  canary, paired operation-database backup, outbox hold, and forward-only gate.
- **Same later rollout verification work unit:** inventory legacy cases and
  verify the exact SPY case/event/outbox delta before holdings materializers,
  NAV preflight, and listener processing resume.
- No live Feishu or production operation-database canary was run in this work
  unit.

## CI and Validation

- Merge-focused holdings/NAV/receipt suites: 117 passed.
- Full repository suite on synchronized head: 1047 passed in 33.43 seconds.
- Python compile and branch diff checks: passed.
- GitHub `quality-contract`: passed in 25 seconds on exact source head
  `cacaf82b3ecafa7932c3d35c8a23a1885217c875`, run `30684632756`.
- Remote PR head equals the reviewed local source head.
- GitHub reports the Draft PR open, mergeable, and clean against current main.

## Documentation Decision

No public schema, HTTP, CLI, or user documentation change is required. The PR
body and Gateflow artifacts already record the private compatibility contract
and forward-only rollout boundary.

## Completion Status

PR re-review: pass. No fix/re-review loop is required. The next Gateflow step is
to commit and push this review/closeout evidence while leaving PR #41 as Draft.
