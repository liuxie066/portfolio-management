# Code Review

## Scope

- Mode: current changes
- Branch: `fix/cash-observation-only`
- Base: `origin/main@02ce7f8b549f`
- Output file: `docs/reviews/code-review-20260801-110017.md`
- Included scope: complete goal/plan, S1 and S2 implementation, full Futu
  synchronization and balance-only paths, durable receipt semantics, retry and
  business-success behavior, quality/NAV/API consumers, legacy receipt skew,
  notification wording, documentation, and tests
- Excluded scope: live OpenD/Feishu execution, production artifacts and data,
  release/deploy, timer/service changes, NAV History execution, and historical
  cash-flow effect data repair
- Parallel review coverage: 无；the complete diff was reviewed end-to-end from
  public sync and NAV entry points through storage readback, durable evidence,
  quality policy, public freshness, and receipt rendering

## Findings

未发现实质性问题。

The aggregate review verified these adversarial paths:

- Futu cash amounts differ materially from PM aggregate CASH: the amounts never
  enter a holdings equality comparison, no foreign CASH holding is read, no
  cash-only wait occurs, and the aggregate structural verdict remains explicit.
- Aggregate CASH is missing, malformed, or repository-unavailable: the durable
  reconciliation is non-trusted and official NAV/public quality fail closed,
  while an otherwise completed Futu write is not falsely reported as rolled
  back.
- Stock/MMF readback mismatches: the existing one-time read-only retry and
  business failure behavior remain intact.
- Old durable receipt or old published artifact: `pm.securities_cash` is not
  aliased to the new semantic contract, so current consumers report missing
  `pm.cash_aggregate` evidence.
- Real Futu sync and sync-first NAV: neither path imports or calls the Futu CASH
  effect bridge, and the receipt ignores even a supplied legacy `cash_effects`
  payload.
- Cash-flow ledger effects: their explicit preview/confirmation and NAV gates
  remain independent; only Futu's automatic broker-drift authority is removed.

Architecture and semantic ownership remain coherent: the provider owns raw
per-currency observation, the reconciler owns stock/MMF replica checks plus the
distinct local aggregate-shape fact, quality policy owns consumer eligibility,
and the renderer only explains those results. No FX conversion rule, production
migration, or new write authority was invented.

## Open Questions

- 无。

## Residual Risk

- The implementation proves the aggregate row's structure, not its economic
  amount. Existing cash-flow/operator controls remain the numeric authority.
- A future deployment must produce a new full Futu receipt before official NAV
  can pass the new dataset contract; old receipts intentionally fail closed.
- Live provider, Feishu, notification, and deployment checks were not run because
  this work unit has no production/release/upgrade authorization.
