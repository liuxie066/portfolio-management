# Code Review

## Scope

- Mode: current changes (Gateflow aggregate deepreview)
- Branch or PR: `codex/fix-holdings-case-precondition-identity`
- Base: `main@2a1ae72a99da1905abe0599c02c496077c63aa91`
- Reviewed head: `8cc4fa75c02ae1d9f397d9cec30d60c8184d54c5`
- Output file: `docs/reviews/code-review-20260801-105404.md`
- Included scope: the complete accepted plan and implementation commits; semantic case identity; precondition v2 and legacy comparator; confirmation scope; normal and combined SQLite materialization; direct resolve/apply; NAV dry-run and formal preflight; Futu evidence outage; event-inbox transaction; closure/discovery/migration receipts; retry, reopen, rollback, idempotency, and test sufficiency
- Excluded scope: live Feishu or production data mutation, listener redesign, timestamp/tag cleanup, release, remote upgrade, and unrelated untracked `docs/reviews/code-review-20260801-084655.md`
- Parallel review coverage: 无；the aggregate was reviewed as one end-to-end compatibility and durable-state work unit

## Findings

未发现实质性问题。

The aggregate review followed the real entry paths from manual notify, event
worker, direct resolve/apply, and NAV preflight into workflow planning and both
SQLite materialization implementations. The semantic case-key payload remains
unchanged. The shared contract removes raw `asset_type` only from fields whose
validator does not consume it, while `currency` and `asset_class` retain the
dependency and require an exact fresh-record legacy comparator before migration.

The compatibility branch is one-way, format-recognized, state-gated, and
semantic-identity-gated. Unknown contracts, different v2 digests, relevant
dependency drift, stale keep scopes, and applying/recovery states remain hard
failures. Eligible migration, keep-scope rewrite, evidence refresh/reopen,
event completion, apply preparation, and existing receipt effects remain under
the caller's `BEGIN IMMEDIATE` transaction. Migration itself emits one audit
event and no operation receipt.

The exact SPY regression proves that correcting `asset_type` and `asset_name`
closes only the obsolete name case while legacy timestamp cases keep their keys
and states. The NAV, outage, event, direct action, dependency mismatch,
in-flight rejection, receipt cardinality, rollback, and repeat-idempotency tests
cover the remaining material branches. No source or test changed after the
accepted-slice validation.

## Open Questions

- 无。

## Residual Risk

- **Assigned to a later authorized release/upgrade work unit:** once any v2
  digest is persisted, the preceding binary cannot consume that row. The later
  rollout must retain the accepted suspended-canary, paired operation-database
  backup, outbox hold, and forward-only gate. No release or remote mutation is
  authorized by this review.
- **Assigned to the same later rollout verification work unit:** for fields that
  do not depend on `asset_type`, the old broad digest cannot reconstruct which
  historical raw type produced it. The current implementation bounds this
  intentional compatibility choice with exact semantic identity, a recognized
  digest shape, eligible states, one-way migration, audit evidence, and the SPY
  regression. The later canary must inventory legacy state and verify the exact
  case/event/outbox delta before materializers resume.
- **Uncovered area assigned to the later rollout work unit:** no live Feishu or
  production operation-database canary was run in this aggregate review.

## Validation

- Aggregate focused rerun: 126 passed.
- Accepted-slice full baseline: 1038 passed, recorded in
  `docs/gateflow/holdings-case-precondition-identity/implementation-s1-s4.md`.
- `git diff --check main...HEAD`: passed.
- Branch contains the accepted plan and implementation commits only; the one
  unrelated untracked review artifact remains excluded.

## Documentation Decision

No public schema, HTTP, CLI, or user documentation change is required. The
private diagnostic token and forward-only rollout boundary are documented in
the accepted Gateflow plan and implementation artifact.

## Completion Status

Aggregate deepreview: pass. No fix or re-review loop is required. Next Gateflow
entry point: accepted deepreview commit.
