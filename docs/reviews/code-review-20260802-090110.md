# Code Review

## Scope

- Mode: current changes
- Branch or PR: `gateflow/feishu-bitable-contract-repair`
- Base: `803bf3b`
- Output file: `docs/reviews/code-review-20260802-090110.md`
- Included scope: complete uncommitted S11 transactions archive diff, refined plan and implementation artifacts, structural registry, generated schema projection, storage/repository/mixin read and mutation paths, domain/archive models, and focused/full test evidence
- Excluded scope: accepted S1-S10 commits; compensation mirror S12; live Feishu schema or business-data mutation; unrelated untracked `docs/reviews/code-review-20260801-084655.md`
- Parallel review coverage: 无；review followed the transaction facade/repository/client paths, registry-to-generated-doc projection, archive validation model, and caller/test surfaces directly

## Findings

### 1-未修复-中-注册表复合业务键未重新生成到 schema 投影，唯一真源的零漂移检查已失败
- **入口/函数**: `scripts/generate_feishu_schema_docs.py --check` -> `_render_contract_block()` -> `docs/schema.md` generated contract block
- **文件(行号)**: `src/feishu/contracts/registry.py:266-271`; `scripts/generate_feishu_schema_docs.py:21-35,86-98`; `docs/schema.md:140-144`; `docs/gateflow/feishu-bitable-contract-repair/implementation-plan.md:32-36`
- **输入场景**: S11 将 transactions archive identity 从全局 `request_id` 修正为 account-scoped `(account, request_id)` 后，开发者、schema audit 或 CI 消费生成的 `docs/schema.md`，或执行生成器的 `--check`。
- **实际分支**: runtime registry 返回 `("account", "request_id")`，但本 slice 未重新运行 schema generator；生成文档仍写 `Business key: request_id`。`python3.12 scripts/generate_feishu_schema_docs.py --check` 实际输出 `generated Feishu schema docs are stale` 并以状态 1 退出。
- **预期行为**: 按已验收 work-unit success signal，registry 是结构唯一真源，`docs/schema.md` 的 generated block 必须是它的零漂移投影；S11 的复合键变更应同时出现在生成文档中，并通过 `--check`。
- **实际行为**: runtime lookup/tests 与公开 schema 文档承诺两种不同的 request-id scope，且仓库自带的漂移检查明确失败。
- **直接证据**: registry 在 `business_key=("account", "request_id")`；generator 第 30-35 行直接从 `table.business_key` 渲染；当前 schema 第 143 行仍只有 `request_id`；验收计划第 35 行要求 generated blocks 与 registry 零漂移。
- **影响**: S11 尚未满足结构唯一真源的完成信号。依赖 schema 文档设计查询、审计或后续迁移的调用者会继续把 request_id 误解为全局唯一；同时任何启用 generator check 的交付门禁都会失败。
- **建议改法和验证点**: 将 `docs/schema.md` 作为 S11 的受控 scope correction，使用现有 generator 更新 generated block，不手改投影；新增或保留 `python3.12 scripts/generate_feishu_schema_docs.py --check` 作为 S11 验证，确认 transactions 显示 `account`, `request_id` 且人写 policy 区域未变化。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

- 无。

## Residual Risk

- Review 未执行 live Feishu archive read；现有 evidence 证明的是已观测 schema 和离线严格 reader，不证明所有历史行都已具备 request_id/dedup_key。按 S11 设计，缺失行会显式失败而不是被默认化。
- `transactions` 仍通过 Feishu 的远端 filter 完成列表筛选，服务端响应之后没有事务快照；request-id 精确查询会本地复核 scope/duplicates，但普通列表读取只对每行字段做严格解析。
- No subagents were used; S11 is a bounded slice and the complete changed call chain was reviewed directly.
