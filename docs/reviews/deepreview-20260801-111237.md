# Aggregate DeepReview

## Scope

- Mode: complete work unit from base
- Work unit: `daily-nav-holdings-receipt-aggregation`
- Branch: `codex/aggregate-daily-nav-holdings-receipts`
- Base: `origin/main@02ce7f8`
- Reviewed commits: `3c8643e`, `d3d46f3`
- Output file: `docs/reviews/deepreview-20260801-111237.md`
- Included: plan evidence, receipt persistence boundary, Daily NAV orchestration,
  account/global propagation, rendering, failure behavior, and tests

## Findings

### DR-AGG-01-未修复-中-全局预检的任务级唯一传播没有端到端回归保护
- **入口/函数**: `DailyNavJobService.run()` ->
  `global_holdings_preflight` ->
  `NavHistoryReceiptService._holdings_preflight_rows()`
- **文件**: `src/app/daily_nav_job_service.py`、
  `src/app/nav_history_receipt_service.py`、`tests/test_daily_nav_services.py`、
  `tests/test_nav_history_receipt_service.py`
- **输入场景**: 全局 orphan Case 在 fresh scan 中被关闭，或全局 blocker 被复制到
  多个 account compatibility item；最终应只在一张 NAV 回执中出现一次全局汇总。
- **实际覆盖**: preflight 测试证明 global workflow 本身可建/关 Case；renderer
  测试只覆盖 account 聚合；job 测试没有断言成功/阻断 global result 被保留到
  top-level。没有一个测试跨越这两个新契约边界。
- **预期行为**: job 必须保留 exact global preflight result，renderer 必须只消费
  top-level global contract，即使每个 account item 含兼容副本也不能重复显示。
- **为什么有问题**: 这是本 work unit 新增且计划明确要求的唯一全局 ownership
  规则；任一字段改名、early return 或 renderer fallback 都可能静默丢失/倍增全局
  通知，而当前全量 suite 仍会通过。
- **影响**: 全局 blocker/repair 可能无提示或按账户重复，重新引入用户正在解决的
  消息噪声与作用域误解。
- **建议改法和验证点**: 新增 DailyNavJob regression，断言 exact global result
  保存在 `global_holdings_preflight`；新增 renderer regression，在两个 account
  compatibility copies 存在时断言 `全局` 关闭/阻断汇总只出现一次且位于账户明细
  前。
- **修复风险**: 低
- **严重程度**: 中

## Confirmed boundaries

- Daily NAV is the only caller selecting `enqueue_receipts=False`.
- Store defaults preserve standalone manual and event behavior.
- Suppression changes only receipt insertions; state transitions and events stay
  in the original transactions.
- Account success, blocker, snapshot/record exception, and report-payload
  exception paths retain aggregate facts.
- Action items preserve exact commands, exclude confirmed cases, and are capped
  independently at producer and renderer.
- Existing NAV outbox remains the single durable task envelope.

## Residual risk

- The accepted cross-transaction crash window remains unchanged.

## Conclusion

`fail`. Accept and fix DR-AGG-01, rerun focused/full validation, then perform
aggregate re-review.
