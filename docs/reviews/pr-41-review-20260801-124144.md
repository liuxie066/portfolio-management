# Code Review

## Scope

- Mode: PR
- Branch or PR: [#41 Fix holdings case precondition identity](https://github.com/liuxie066/portfolio-management/pull/41)
- Author: `liuxie066`
- Head: `codex/fix-holdings-case-precondition-identity@d0bf47a1725c8320b76e0de3d5d0324f28582c75`
- Base: `main`
- Output file: `docs/reviews/pr-41-review-20260801-124144.md`
- Included scope: PR title/body accuracy, complete remote diff, semantic case identity, precondition v2 compatibility, store transactions, manual/event/NAV/outage call paths, case/event/outbox consistency, tests, rollout wording, and GitHub checks
- Excluded scope: live Feishu or production database mutation, release, remote upgrade, merge, approval, reviewer requests, and unrelated local untracked `docs/reviews/code-review-20260801-084655.md`
- Parallel review coverage: 无；the PR is the already accepted cohesive compatibility and durable-state work unit

## Findings

未发现实质性问题。

The remote PR diff matches the accepted local head and contains only the
Gateflow plan/review artifacts, the field-specific precondition implementation,
and its regression coverage. The PR description accurately states the root
cause, behavior change, no-holdings-write boundary, validation evidence, and
forward-only rollout constraint without claiming a release or deployment.

The final diff retains the v1 semantic case-key payload, recognizes only the
known unprefixed legacy digest format, and migrates only eligible same-semantic
cases to a recognized v2 digest. `currency` and `asset_class` keep the raw
`asset_type` dependency and require an exact fresh legacy comparator. Unknown
contracts, v2 mismatches, stale keep scopes, relevant dependency drift, and
applying/recovery states remain fail-closed.

Both materialization paths perform the transition inside their existing SQLite
transaction. Migration produces an audit event but no user-facing operation
receipt; closure, reopen, apply preparation, event completion, and receipt
effects remain atomic. The SPY, NAV, outage, direct action, event, rollback, and
idempotency tests exercise the material branches.

## Open Questions

- 无。

## Residual Risk

- **Assigned to a later authorized release/upgrade work unit:** the preceding
  binary cannot consume a persisted v2 digest. The later rollout must use the
  documented suspended canary, paired operation-database backup, outbox hold,
  and forward-only gate.
- **Assigned to the same later rollout verification work unit:** non-dependent
  legacy digests cannot reconstruct the historical raw `asset_type`. The
  one-way compatibility guard and exact SPY regression bound that accepted
  limitation; the live canary must verify exact case/event/outbox deltas before
  materializers resume.
- **Uncovered area assigned to the later rollout work unit:** this PR review did
  not run a live Feishu or production operation-database canary.

## CI and Validation

- GitHub check `quality-contract`: passed in 27 seconds.
- Aggregate focused suite: 126 passed.
- Accepted-slice full repository baseline: 1038 passed.
- Remote PR head equals the reviewed local head.
- `git diff --check origin/main...HEAD`: passed.

## Documentation Decision

No public schema, HTTP, CLI, or user documentation change is required. The PR
body and committed Gateflow artifacts record the private token and rollout
boundary.

## Completion Status

PR review: pass. No fix or re-review loop is required. Next Gateflow entry
point: accepted PR review commit, followed by final push.
