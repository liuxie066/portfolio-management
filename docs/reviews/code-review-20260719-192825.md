# Code Re-review — repo-review-27 S7

## Scope

- Mode: current changes re-review
- Branch or PR: `gateflow/fix-repo-review-27`
- Base: `c76b916`
- Source review: `docs/reviews/code-review-20260719-192824.md`
- Output file: `docs/reviews/code-review-20260719-192825.md`
- Included scope: accepted finding 1 fix plus the complete S7 service fallback, POST ambiguity, ASGI admission, wrapper environment, tests, and docs diff.
- Excluded scope: deployment/release/live requests, new authentication/status products, and unrelated modules.
- Parallel review coverage: 无。

## Findings

未发现实质性问题。

## Accepted Finding Re-review

- Finding 1: `已修复` — `_service_or_fallback()` has no safety default; AST inspection confirms all 12 call sites pass `allow_fallback` explicitly, with write-capable commands false and read-only commands true.

## Adversarial Failure Pass

- Read unavailability performs one direct fallback; write unavailability and POST transport timeout perform none.
- `--no-service` bypasses client construction and invokes the direct backend once.
- Official daily-report publication propagates `PortfolioServiceOutcomeUnknown` instead of entering its legacy unavailable fallback.
- IPv4/IPv6 loopback pass; a remote actual client receives 403 even with spoofed loopback Host and X-Forwarded-For.
- Module-level app remains fail-closed without explicit environment; explicit remote override works.
- Wrapper environment is set during Uvicorn execution/reload inheritance and restored after return.

## Project Instructions Pass

- No live mutation, deployment, release, merge, PR-ready transition, reviewer request, or unrelated cleanup occurred.
- The fix is the smallest shared-boundary change that makes unsafe omission impossible; no dependency or speculative abstraction was added.

## Overcoupling and Semantic Ownership Drift

- Fallback classification is explicit at each command entry point and enforced by the shared helper signature.
- POST ambiguity stays in the transport client; operator presentation stays in CLI error handling.
- Network admission stays in ASGI middleware and does not move trust to proxy headers or launcher-only checks.

## Validation

- Required S7 focused suite -> `67 passed`.
- Focused service CLI/HTTP isolation -> `8 passed`.
- Full repository suite -> `657 passed`.
- `python3 -m compileall -q src scripts skill_api.py` -> passed.
- `git diff --check` -> passed.
- AST fallback classification -> 12/12 call sites explicit; 4 writes false, 8 reads true.
- Official daily-report adversarial probe -> direct fallback call count zero.

## Open Questions

无。

## Residual Risk

- Explicit remote mode remains unauthenticated and is `documented explicit operator risk`.
- Ambiguous write result inspection remains operator-driven; a status API is `assigned to a later work unit if operational evidence requires it`.

## Decision

- Gate result: `code review pass`.
- Accepted findings: `1`, final state `已修复`.
- Open questions: none.
- Residual risks: classified.
- Next gate: `accepted slice commit`.
