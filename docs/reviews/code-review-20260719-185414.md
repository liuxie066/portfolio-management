# Code Review — repo-review-27 S5

## Scope

- Mode: current changes
- Branch or PR: `gateflow/fix-repo-review-27`
- Base: `67135d3`
- Output file: `docs/reviews/code-review-20260719-185414.md`
- Included scope: valuation fallback semantics, cache-only purity, monotonic deadline propagation, provider/FX retry and timeout paths, numeric validation, fixed cash/MMF/crypto pricing, pricing symbol canonicalization, US market time, snapshot quantity precision, and focused regressions.
- Excluded scope: approved S6-S7 findings, release/deployment/live data, exchange-holiday calendar expansion, historical cache migration.
- Parallel review coverage: 无。

## Findings

### 01-已修复-[中]-canonical code 在 provider routing 前丢失显式市场后缀语义
- **入口/函数**: `PriceService.fetch_quote()` / `BatchPricePlanner.fetch_batch()` -> provider `supports()`
- **文件(行号)**: `src/pricing/service.py:82-83,182-194`; `src/pricing/batch.py:55-61,122-135`; `src/pricing/providers/fund.py:16-26`; `src/pricing/providers/hk.py:17-19`
- **输入场景**: 调用方只提供带显式后缀、但去后缀后会被启发式分类到另一市场/类型的代码，例如 `004001.SH`；或提供显式 `.US` 的纯数字 symbol 且没有 `asset_type_map`。
- **实际分支**: pricing boundary 先把代码 canonicalize 为 `004001`/纯数字，再用 canonical code 重新执行基金/HK 启发式；原始 `.SH/.SZ/.HK/.US` 已不再参与 provider 选择。`FundProvider` 也不会因显式 stock asset type 排除基金路径，`HKStockProvider` 对纯数字无条件支持。
- **预期行为**: 后缀只应从 provider query/cache key 中移除；它表达的市场事实必须保留并优先于去后缀后的数字/名称启发式。
- **实际行为**: 合法显式市场输入仍可能查询错误 provider/symbol，finding 22 只对常见样例修复，未形成完整 canonical boundary contract。
- **直接证据**: `canonicalize_pricing_code()` 的结果覆盖原始 code；batch grouping 调用 `detect_market_type(canonical)`；single path在没有 map 时不给 provider 传递从原始后缀推导的 type；provider `supports()` 继续仅按 canonical 数字/名称判断。
- **影响**: 返回错误类型行情或缺价；显式后缀不能可靠覆盖启发式，调用方的确定性市场信息被静默丢弃。
- **建议改法和验证点**: 在 canonicalize 前从原始代码保留 market/type hint；batch grouping 使用原始代码的 market fact，single path把显式后缀推导的 asset type传给 provider；Fund/CN/HK supports 在存在显式 asset type 时优先遵守它。增加 ambiguous `.SH` 与 numeric `.US` 路由回归，同时继续证明 `BRK.B` 不被剥离。
- **修复风险（低/中/高）**: 中
- **严重程度（低/中/高/严重）**: 中

## Open Questions

无。

## Residual Risk

- Exchange holidays remain outside the approved market-time contract.
- Historical suffixed cache keys are not migrated; this is already classified in the S5 implementation artifact.
- Full repository suite passed, but external providers were validated with deterministic fakes rather than live network calls.

## Decision

- Gate result: `code review fail`.
- Accepted findings: 1.
- Next gate: preserve explicit market/type facts across canonicalization, add adversarial routing tests, run focused validation, and re-review.


## Re-review

- Re-review time: `2026-07-19 18:58:58 +0800`
- Finding `01`: `已修复`.
- Fix evidence:
  - `PriceService.fetch_quote()` and `PriceFetcher._fetch_realtime()` derive an explicit `asset_type` from the original `.SH/.SZ/.HK/.US` suffix before provider routing, while the canonical code remains the cache/provider query key.
  - `BatchPricePlanner.fetch_batch()` derives the canonical asset type from the primary original code and calls `detect_market_type(primary)`, so a numeric `.US` symbol does not enter CN/HK heuristics.
  - CN/ETF/Fund/HK provider `supports()` methods reject explicit conflicting asset types instead of overriding them with code-shape heuristics.
  - Regressions cover ambiguous `004001.SH`, numeric `510300.US` in single and batch paths, and continued preservation of `BRK.B`.
- Adversarial failure pass: no remaining path in the reviewed single or batch entry points discards an explicit supported terminal suffix before market selection.
- Project-instruction pass: no release, deployment, live-data mutation, new dependency, or unrelated cleanup was introduced.
- Overcoupling / semantic-ownership-drift pass: suffix interpretation stays at the pricing entry boundary; providers only enforce the received authoritative asset type and do not add a second canonicalization policy.

## Re-review validation

- Required S5 focused suite -> `63 passed`.
- Full repository suite -> `635 passed`.
- `python3 -m compileall -q src tests` -> passed.
- `git diff --check` -> passed.
- Pricing worker/executor search -> no `ThreadPoolExecutor`, `as_completed`, `threading.Thread`, or `daemon=True` matches in the S5 pricing paths.

## Final Decision

- Gate result: `code review pass after fix and re-review`.
- Accepted findings remaining: `0`.
- Finding `01`: `已修复`.
- Open questions: none.
- Residual risks remain classified in the S5 implementation artifact.
- Next gate: `accepted slice commit`.
