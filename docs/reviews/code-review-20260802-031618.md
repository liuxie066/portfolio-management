# Code Review

## Scope

- Mode: current changes final S4 re-review
- Branch or PR: `gateflow/feishu-bitable-contract-repair`
- Base: `8ef9c8c`
- Output file: `docs/reviews/code-review-20260802-031618.md`
- Included scope: complete S4 cash-flow raw/manual/completed contracts, complete list and exact projections, direct create and replay, aggregate cache publication and invalidation, summary fallback, effect-source revisions, reconcile compatibility, and every fix from `code-review-20260802-025828.md`
- Excluded scope: committed S1-S3 behavior unrelated to cash-flow contracts; planned S5 duplicate/FX/readback sequencing and S6 run-scoped NAV dataset; unrelated untracked `docs/reviews/code-review-20260801-084655.md`; live Feishu/Futu calls; merge, release, and deployment
- Parallel review coverage: none; developer instructions prohibit unrequested delegation, so the primary reviewer independently re-walked raw reads, promotion, replay identity, direct create, hot/fresh aggregates, reconcile, and effect correction

## Findings

No actionable findings.

The final review specifically rechecked:

- Raw exact/list reads request the registry-complete field set and preserve
  observed missing currency, flow type, dedup, source, remark, and updated-at.
- Manual and completed promotion share one Decimal-based contract for date,
  identity, supported currency, nonzero amount, sign, rate, CNY formula,
  persisted dedup, and source.
- Direct write rejects transport/partial facts before lookup or mutation.
  Replay requires an exact completed row whose observed dedup still equals the
  request, and stale local mappings cannot suppress a different transaction.
- Loaded cache append and fresh aggregate rebuild now use the same Decimal
  summation. Unloaded, malformed, reconciled, or deleted cache scopes are
  invalidated without publishing partial financial totals.
- Missing or invalid dates and all other incomplete rows block before aggregate
  publication, leaving the prior cache untouched.
- Summary and effect services promote the same completed facts. Effect hashes
  retain CNY/rate/dedup/source/remark/updated-at changes, while invalid observed
  dedup remains visible as a blocked revision.
- The canonical dedup helper matches historical Pydantic-float fingerprint text
  for ordinary and scientific-notation values without moving financial
  calculation away from Decimal.

No alternate defaulting write path, foreign-currency amount fallback,
float-only hot-cache calculation, or existence-only replay branch remains in
the reviewed S4 scope.

## Validation Evidence

- Focused contract/storage fault suite: 45 passed.
- Final S4 scoped and compatibility suite: 175 passed.
- Full repository suite: 1204 passed.
- Python compileall, scoped Ruff, and `git diff --check`: passed.
- No live Feishu or Futu request was made.

## Open Questions

None.

## Residual Risk

- S5 owns expected-dedup duplicate groups, FX evidence binding, generated patch
  sequencing, and fresh write readback; these remain planned, not hidden S4
  assumptions.
- S6 owns one immutable run-scoped cash-flow dataset for official NAV and removes
  the remaining persistent-cache/double-scan authority split.
- Live business-row conformance remains unknown until the separately authorized
  read-only pre-production audit. No historical repair is authorized here.
- Cross-system create/edit races cannot be eliminated without server-side
  conditional writes; the later concurrency work unit remains the owner.
