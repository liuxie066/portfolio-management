# Code Review Re-review

## Scope

- Mode: current changes re-review
- Branch: `fix/nav-finality-write-authority`
- Base: `b5e3989`
- Prior review: `docs/reviews/code-review-20260721-115304.md`
- Fix artifact: `docs/gateflow/nav-finality-write-authority/fix-s1-review.md`
- Output file: `docs/reviews/code-review-20260721-115858.md`
- Reviewed scope: fixes for DR-S1-01 through DR-S1-03 plus affected finality/state-machine tests.
- Parallel review coverage: 无.

## Finding re-review

### DR-S1-01-已修复-[高]-读取侧未按写入侧契约验证 finality

- Construction and parsing now share `FINALITY_STATUS_BY_WRITER`.
- Unknown writers and incompatible final writer/status combinations fail closed.
- `valuation_as_of` is required in version 1 and any non-null value must parse as ISO datetime.
- Explicit empty run IDs are rejected.
- Canonical final daily-job and honest final nav-repair rows remain eligible.
- Regression coverage exercises unknown writer, close-nav/final mismatch, invalid timestamp, and missing timestamp.

Final status: `已修复`.

### DR-S1-02-已修复-[中]-运行时字段冲突被静默吞掉

- `with_runtime()` now rejects conflicting non-empty run IDs and valuation timestamps.
- Matching or missing runtime fields continue through immutable replacement.
- Both conflict branches have direct tests.

Final status: `已修复`.

### DR-S1-03-已修复-[中]-datetime nav_date compatibility regression

- `NavRecordService` normalizes datetime to date before all calculations and context comparison.
- Direct datetime input now persists a date-aligned finality contract.
- Regression test passes.

Final status: `已修复`.

## Adversarial re-check

- Snapshot failure and unresolved compensation still override otherwise valid finality.
- Legacy/manual/mismatched rows remain blocking.
- Writer remains provenance rather than canonicality truth: both daily-job and validated nav-repair final rows qualify.
- Invalid contract combinations cannot be constructed through `NavWriteContext` and cannot qualify when read from storage.
- Runtime trace conflicts fail before persistence.

## Validation

```text
73 passed in 0.34s
compileall: pass
git diff --check: pass
```

## Open Questions

- 无.

## Residual Risk

- Repository mutation serialization and safe defaults remain classified under approved S2.
- Existing production legacy rows remain operator-owned remediation; no auto-promotion was introduced.
- Cross-host serialization and provider quote-date proof remain outside this work unit/slice as previously classified.

## Conclusion

`pass` — all accepted S1 findings are `已修复`; no blocking findings remain. Slice S1 is ready for its protected local acceptance commit.
