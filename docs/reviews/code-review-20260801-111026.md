# Code Re-review

## Scope

- Mode: current S1 changes after fixes
- Work unit: `daily-nav-holdings-receipt-aggregation`
- Prior review: `docs/reviews/code-review-20260801-110726.md`
- Fix artifact: `docs/gateflow/daily-nav-holdings-receipt-aggregation/s1-code-review-fix.md`
- Branch: `codex/aggregate-daily-nav-holdings-receipts`
- Base: `origin/main@02ce7f8`
- Output file: `docs/reviews/code-review-20260801-111026.md`

## Finding dispositions

### DR-S1-01 — fixed

`pending_case_keys` now excludes exact confirmed cases, action items are filtered
to that pending set, and the renderer consumes the new contract with a legacy
fallback. The existing keep-current flow is covered through a complete
materialize -> resolve -> later preflight regression and produces neither a
pending count nor a repeated command.

### DR-S1-02 — fixed

The renderer now caps action items independently, safely handles malformed
counts and entries, and derives a conservative omitted count. An oversized
seven-item payload with invalid numeric fields still builds the core receipt,
shows five items, and reports two omitted.

## Additional boundary checks

- `enqueue_receipts` defaults to `True` at store and workflow boundaries.
- Only account/global Daily NAV preflight passes `False`.
- Event inbox internal transaction calls retain the default and focused event
  regressions pass.
- Manual notify/resolve/apply/recover call sites retain the default and their
  existing receipt regressions pass.
- Case/Event writes remain inside their original SQLite transactions; only
  operation-outbox insertion is conditional.
- Account post-preflight exceptions and report-payload exceptions retain a
  sanitized aggregate contract.
- Global preflight is held once at task scope and the renderer does not inspect
  compatibility copies placed in per-account global blocker items.

## Validation

- Focused suite after fixes: `116 passed`
- Full suite after fixes: `1021 passed`
- Compile check after fixes: passed

## Findings

No unresolved findings.

## Residual risk

- The accepted Case/Event-commit to NAV-envelope-enqueue crash window remains;
  durable audit state survives.

## Conclusion

`pass`. S1 is accepted and ready for its scoped commit followed by aggregate
deepreview.
