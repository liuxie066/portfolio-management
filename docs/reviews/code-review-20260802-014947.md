# Code Re-Review

## Scope

- Mode: current changes re-review
- Branch: `codex/feishu-dual-app-credentials`
- Base: accepted S3 commit `4185aa2`
- Prior review: `docs/reviews/code-review-20260802-014847.md`
- Fix artifact: `docs/gateflow/feishu-dual-app-credentials/s4-fix.md`
- Output file: `docs/reviews/code-review-20260802-014947.md`
- Included scope: S4 fixes plus the complete operator/configuration contract and
  focused documentation tests
- Excluded scope: live credentials, systemd apply, Feishu API calls,
  subscription, service activation, release, and deployment
- Parallel review coverage: 无；slice is bounded to operator contracts and tests

## Findings

未发现新的实质性问题。

## Prior finding status

### S4-CR-01 — 已修复

The migration state prepares only checkout/venv first, provisions both named
encrypted credentials second, applies credential-bearing system assets third,
and then runs secure preflight. This is executable with the installer's
credential-before-write gate, and the ordering is covered by a regression test.

### S4-CR-02 — 已修复

The Conversation role now names only the exact least-privilege
`im:message:send_as_bot` permission. The vague broader substitute was removed
and a regression test rejects its return.

## Evidence

- Installer/documentation suite: `31 passed`.
- `git diff --check`: passed.
- Manual trace: migration runbook order matches `apply_install()` credential
  presence/capability checks before target writes.
- Permission table retains exactly two applications and no third event role.

## Open Questions

- 无。

## Residual Risk

- Tenant-side permissions, credential decryption, message delivery, Base
  subscription, and listener health require separately authorized live checks.
- Plaintext cleanup remains intentionally manual and separately authorized.

## Conclusion

`pass`

All accepted S4 findings are fixed and the slice may be committed.
