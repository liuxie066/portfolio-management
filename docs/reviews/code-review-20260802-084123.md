# Code Review

## Scope

- Mode: current changes aggregate re-review
- Branch or PR: `gateflow/feishu-bitable-contract-repair`
- Base: `dc807bc`
- Output file: `docs/reviews/code-review-20260802-084123.md`
- Included scope: complete uncommitted S10 snapshot exact-set work unit after
  fixing DR-S10-01 through DR-S10-06; typed plan and authority contracts;
  normal, initialization, and CLOSED entry points; account-lock ownership;
  Feishu fresh snapshot read/create/update/clear/delete boundary; digest-covered
  NAV recovery transition; original and compensation state machines; public
  result classification; tests and Gateflow implementation/fix artifacts
- Excluded scope: unrelated untracked
  `docs/reviews/code-review-20260801-084655.md`; S11 transaction archive and S12
  aggregate registry work; live Feishu/Futu reads or writes; live schema
  mutation; historical rewrite; deployment
- Parallel review coverage: 无；the primary reviewer traced all six accepted
  findings and the normal/dry-run/init/CLOSED plus original/retry state paths

## Findings

无。

## Accepted Finding Verification

- DR-S10-01: dry-run planned/false is a successful public preview and exposes
  status plus exact mutation preview; real-write failure classification is not
  invoked.
- DR-S10-02: CLOSED partial state propagates failure, task id, and retry command
  instead of returning unconditional success.
- DR-S10-03: the account lock begins before history loading and calculation for
  normal and CLOSED writes. The internal persistence helper is lock-free, so
  there is no account-lock re-entry. A deterministic concurrent test proves the
  second same-account writer observes the first NAV.
- DR-S10-04: every exact-set-owned optional snapshot field is clearable in the
  typed registry and is accepted by the actual update validator.
- DR-S10-05: recovery targets validate prepared/complete semantics and digests,
  and fresh NAV base drift is rejected before snapshot mutation in both the
  original and retry paths. Completion patches occur only after a fresh full
  base match. No raw recovery helper remains that can bypass NAV-state
  classification.
- DR-S10-06: overwrite intent, run id, and issuer must agree between NAV context
  and snapshot authority before snapshot planning or any NAV write preview.

## Open Questions

无。

## Residual Risk

- Feishu still has no cross-table transaction or remote compare-and-swap.
  External edits can race after a validated fresh read and before mutation;
  observed drift fails closed or is caught by final readback, but strict remote
  atomicity cannot be guaranteed.
- A NAV timeout followed by an immediately absent fresh read still assumes
  no-replay. A delayed remote commit or read-after-write visibility lag remains
  an external protocol risk outside source-only validation.
- No live destructive exact-set or physical Number/null/delete canary was
  authorized, so source tests do not prove Feishu's runtime null-versus-missing
  representation.

## Validation Evidence

- Exact S10 suite: `133 passed`.
- Scoped Ruff and `git diff --check`: passed.
- Full repository suite after the final re-review refinements: `1355 passed`.
- Python compileall: passed.
