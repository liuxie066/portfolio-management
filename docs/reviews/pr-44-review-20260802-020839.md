# Code Review

## Scope

- Mode: PR
- Branch or PR: `liuxie066/portfolio-management#44`, `codex/feishu-dual-app-credentials`, `86e1b93bdf6cc28c62d63a8888a80f6badf3a325`
- Base: `main@59625b0d6c666da338c0a520e85a221932846949`
- Output file: `docs/reviews/pr-44-review-20260802-020839.md`
- Included scope: PR title/body, complete diff, two-role configuration resolution, systemd credential loading and override precedence, installer migration behavior, Bitable/event and Conversation/receipt consumer routing, CLI diagnostics, tests, and operator documentation
- Excluded scope: live Feishu permission/subscription behavior, production systemd apply, credential provisioning/rotation, listener activation, release, deployment, and plaintext cleanup
- Parallel review coverage: 无；the final PR head was reviewed locally as one credential-source and consumer-routing contract

## Findings

### PR-CR-01-未修复-高-两处现行操作文档仍引导用户把 Conversation App Secret 写入旧明文配置

- **入口/函数**: operator follows repository guidance in `SKILL.md` or the Cash Flow receipt runbook while configuring the receipt bot
- **文件(行号)**: `SKILL.md:67`; `docs/cash-flow-holding-effects.md:765-773`; compatibility consumption in `src/config.py:90-95, 282-318`
- **输入场景**: a user configures the new Conversation application after reading either tracked document and supplies `feishu.receipt.app_secret` or `OM_FEISHU_BOT_APP_SECRET` as instructed
- **实际分支**: outside secure mode the same-role legacy alias remains a selectable plaintext compatibility source; inside generated production units secure mode refuses the plaintext fallback when the encrypted Conversation credential is absent
- **预期行为**: every current operator-facing setup path names `feishu.conversation.app_id`, `feishu.conversation.open_id`, and the `pm-feishu-conversation-app-secret` encrypted systemd credential, while legacy secret keys are described only as migration shadows
- **实际行为**: both documents present the legacy plaintext secret key as the active receipt configuration, contradicting the PR's canonical contract and the secure production units
- **直接证据**: `SKILL.md:67` explicitly lists `feishu.receipt.app_secret` and `OM_FEISHU_BOT_APP_SECRET` as receipt configuration; `docs/cash-flow-holding-effects.md:769-770` explicitly lists `feishu.receipt.app_secret`; `_resolve_canonical_secret()` still accepts same-role legacy plaintext only for the non-secure migration window, while secure mode raises `insecure_secret_source` or `missing_secure_credential`
- **影响**: an operator can continue storing a private App Secret in plaintext, or can follow repository instructions and then have secure preflight/services fail; this defeats the user-visible security objective and produces inconsistent migration behavior
- **建议改法和验证点**: update both existing documents to the canonical Conversation identity plus named systemd credential, label old keys as migration-only, and run a tracked-doc search proving no remaining document recommends a plaintext Feishu secret
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 高

## Open Questions

- 无。

## Residual Risk

- GitHub `Quality Contract / quality-contract` was queued at review time; classification: covered by the current PR gate and must be checked before final closeout.
- Live systemd credential injection and Feishu permission/subscription/message canaries were not executed; classification: assigned to the separately authorized deployment/migration runbook and production operator, outside this work unit.
