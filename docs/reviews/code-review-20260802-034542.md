# Code Review

## Scope

- Mode: final S5 re-review
- Work unit: `feishu-bitable-contract-repair` / S5
- Base: `8c9266e`
- Review chain: `docs/reviews/code-review-20260802-033515.md`, `docs/reviews/code-review-20260802-034145.md`, `docs/reviews/code-review-20260802-034431.md`
- Fix artifact: `docs/gateflow/feishu-bitable-contract-repair/s5-fix.md`
- Included: final canonical cash-flow manual/completed/duplicate contracts, reconciliation state machine, FX evidence matching, legacy resolver boundary, CLI behavior, fault injection, and all changed lines from the accepted S4 base
- Validation observed: S5 `120 passed`; full suite `1232 passed`; compileall, scoped Ruff, and `git diff --check` passed

## Findings

No actionable findings.

The final diff now enforces the requested sequence: fresh full-scope scan,
manual validation, expected canonical dedup grouping, patch planning, optional
write, fresh readback, and completed validation. Duplicate groups block every
target member independent of observed dedup text. A generated-field proposal
cannot expose an observed fingerprint, and apply cannot succeed until fresh
readback proves the row unique and complete.

Manual FX evidence is validated before mutation, uses one source authority,
and is bound locally to exact record, flow date, observed generated-field
fingerprint, rate, and CNY amount. Batch and readback failure paths preserve
partial-write evidence and invalidate aggregate cache authority. The old
foreign FX resolver now fails closed.

The duplicate CLI is read-only and has no delete/rewrite path. No live Feishu,
Futu, schema, release, or deployment action was performed.

## Open Questions

- 无。

## Residual Risk

- Feishu has no compare-and-swap across initial scan and batch update. External
  edits in that interval are detected by fresh readback and fail closed, but
  cannot be atomically prevented.
- `OperationStateStore.source_hash` remains the persisted compatibility column
  name for the generated-field fingerprint. The public matcher accepts that
  name as a transport alias; the S5 repository itself emits an explicit
  `generated_fingerprint` and pending proposals emit none.
- Duplicate resolution remains deliberately manual; S5 detects and blocks but
  never chooses, deletes, merges, or rewrites duplicate business rows.
