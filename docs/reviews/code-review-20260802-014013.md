# Code Re-Review

## Scope

- Mode: current changes re-review
- Branch: `codex/feishu-dual-app-credentials`
- Base: accepted S2 commit `61ff84f`
- Prior review: `docs/reviews/code-review-20260802-013859.md`
- Fix artifact: `docs/gateflow/feishu-dual-app-credentials/s3-fix.md`
- Output file: `docs/reviews/code-review-20260802-014013.md`
- Included scope: S3 fix plus installer filtering, shadow reporting, credential
  presence/capability preflight, unit grants, disabled local preflight, direct
  script execution, and focused tests
- Excluded scope: S4 runbooks, live credential decryption, service activation,
  Feishu calls, release, and remote deployment
- Parallel review coverage: 无；slice is bounded to one installer and tests

## Findings

未发现新的实质性问题。

## Prior finding status

### S3-CR-01 — 已修复

Only `src/configuration/feishu_credentials.py` defines the two runtime credential
names. The installer bootstraps the repository path and imports those constants.
The new external-cwd subprocess regression proves the absolute-path deployment
entrypoint still imports successfully.

## Evidence

- Installer suite: `30 passed`.
- Python compile, `bash -n scripts/install.sh`, and `git diff --check`: passed.
- Credential-name definition search: exactly one definition per role, both in
  the runtime credential module.
- Unit tests prove exact least-privilege grants and capability-before-target-write
  ordering.

## Open Questions

- 无。

## Residual Risk

- Actual encrypted credential decryption remains a deployment preflight action;
  this source slice deliberately performs no service start.
- Plaintext shadow deletion remains separately authorized and is not automated.

## Conclusion

`pass`

All accepted S3 findings are fixed and the slice may be committed.
