# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `plan/holdings-validation-event-trigger`
- Base: `5a9f1f0`
- Output file: `docs/reviews/code-review-20260731-225629.md`
- Included scope: complete S4 implementation and fixes for
  `DR-S4-01`/`DR-S4-02`
- Excluded scope: live external operations, push/PR, release, deployment, and
  production mutation
- Parallel review coverage: none; the main reviewer re-walked both findings
  and the complete official NAV validation-to-valuation path

## Findings

未发现实质性问题。

The prior findings are closed with direct evidence:

- `DR-S4-01`: every formal account preflight now opens the durable workflow
  transaction even with zero current cases, closes absent per-record cases
  only when fresh evidence is complete, and fails closed on state errors. A
  formal empty global orphan scan proves the synthetic case repaired and
  queues its closure receipt; dry-run opens no writable store.
- `DR-S4-02`: an outage path reads only durable `resolved_keep` decisions,
  requires Futu authority, then recomputes raw identity, canonical current
  value, authority inputs, policy, precondition, and confirmation scope. Only
  exact fields receive manual precedence. Fresh evidence never uses this
  fallback, and drift or unrelated unconfirmed cases remain blockers.

Additional re-review evidence:

- raw discovery includes missing/invalid/nonfinite quantity rows so validation
  cannot be bypassed as `skipped_no_accounts`, while valid zero rows retain the
  existing inactive-account behavior;
- a formal NAV cannot consume Futu dry-run projection data, and the rejection
  occurs before the sync service starts;
- the frozen snapshot owns canonical raw and normalized digests, returns only
  private valuation copies, and is passed explicitly without a storage reread;
- account blockers remain local; one current orphan remains a single global
  blocker/case/receipt; eligible existing final NAV rows skip before the gate;
- NAV details, job results, and receipts carry the same normalized holdings
  digest.

## Open Questions

- 无。

## Residual Risk

- No live Feishu/Futu canary was run under implementation-only authority.
- Cross-host/external Feishu writers are not serialized by same-host locks;
  the frozen digest records the exact source facts consumed by NAV.
- A provider outage cannot reveal newly changed provider facts. Only current
  raw facts and exact durable manual decisions are used during that outage;
  fresh provider evidence supersedes them when service returns.
