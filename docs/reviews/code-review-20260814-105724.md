# Code Review

## Scope

- Mode: current changes re-review
- Branch or PR: `fix/non-futu-cash-holdings-authority`
- Base: accepted S1 commit `6bfd211`
- Output file: `docs/reviews/code-review-20260814-105724.md`
- Included scope: Gateflow S2 implementation plus fixes for DR-S2-01 through
  DR-S2-03; exception ownership, blocker projection, receipt fallbacks, and all
  focused S2 tests
- Excluded scope: accepted S1; unrelated untracked `assets/` and
  `docs/reviews/code-review-20260801-084655.md`; live external systems
- Parallel review coverage: 无；主 reviewer 复核通用 failure、所有
  CASH_FLOW_* refusal 分支、EFFECT_* blockers、跨账户 previous/current
  operations 和 fallback behavior

## Findings

未发现实质性问题。

DR-S2-01 至 DR-S2-03 均已关闭：formatter 只接管 `CASH_FLOW_*` contract；
effect infrastructure failures 使用 Run-ID/gate guidance；account-scoped
operation projection 不再把 current source facts 复用给 previous account，且
无法安全派生时宁可省略 broker/amount 也不输出错误事实。

## Open Questions

- 无。

## Residual Risk

- Receipt 每账户展示第一项并对其余 effect/operation 计数；完整 blocker facts
  保留在 durable result 和 `pm cash-flow review` 输出中。
- 未运行 live Feishu receipt canary；70 个 focused tests、ruff 和 diff check
  通过。
