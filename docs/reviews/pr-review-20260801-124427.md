# PR Review

## Scope

- Pull request: `#42 fix: align Futu cash with aggregate holdings`
- URL: `https://github.com/liuxie066/portfolio-management/pull/42`
- State: open PR
- Base: `main@b36c908faff25ebc19b5d6586e75581d9c3947de`
- Head: `fix/cash-observation-only@e8bd23e3db0a1703a9ea410475074d43d9bb3c6b`
- Output file: `docs/reviews/pr-review-20260801-124427.md`

## Remote scope audit

The remote PR contains the accepted cash-observation-only work unit:

- Futu per-currency cash remains source evidence and is not reconciled against
  synthetic per-currency PM holdings;
- PM cash validity is evaluated from the converted aggregate `CNY-CASH`
  holding and its structural identity;
- automatic Futu-to-CASH-Effects bridging and the corresponding receipt
  counters/actions are removed;
- quality policy, NAV preflight, HTTP output, tests, and operator documentation
  use the same `pm.cash_aggregate` contract;
- Gateflow and review evidence are included.

No version metadata, release workflow, deployment target, remote runtime state,
live holdings data, or NAV History data is changed by this PR.

## Integration review

- The branch was merged with the current `main` after PR creation because
  `main` advanced to the Daily NAV holdings-receipt aggregation change.
- The overlapping account recorder and holdings preflight test changes merged
  without conflicts.
- The PR remains mergeable and its remote file list matches the local
  `origin/main...HEAD` work-unit diff.
- Existing historical Futu cash-flow reconciliation and explicitly confirmed
  cash-flow-ledger effects remain available; only the automatic balance-sync
  bridge is removed.

## Validation at reviewed head

- Full suite on current `main` integration: `1027 passed`
- Ruff on all Python files changed by this work unit: passed
- Python compile check for `src`, `scripts`, and `skill_api.py`: passed
- Diff whitespace check: passed
- Whole-repository Ruff reports 117 pre-existing baseline findings outside the
  work-unit lint scope; no unrelated lint cleanup was included.
- GitHub `quality-contract`: queued at review time; merge remains conditional on
  the check succeeding for the final pushed head.

## Findings

No unresolved findings.

## Conclusion

`pass`. Push this review artifact, then require the GitHub quality check to pass
before merging PR #42 into `main`.
