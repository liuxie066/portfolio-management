# Plan Review (Re-review)

## Scope

- Reviewed target: 上一轮评审 `docs/reviews/plan-review-20260722-130000.md` 三个发现（PR-01/02/03）的修复 + 修复本身引入的回归面
- Work unit: us-pricing-sina-fallback
- Review mode: adversarial Gateflow plan re-review
- Branch: `main`（工作区未提交改动）
- Output file: `docs/reviews/plan-review-20260722-131500.md`
- Reviewed code boundaries: `src/pricing/providers/sina_us.py`（`_sina_query_code` 及其全部调用点）、`us.py`、`us_batch.py`、新增三个测试、`docs/architecture.mmd`、`decisions/2026-07-22-us-pricing-sina.md`；全量 pytest；本机实盘 smoke（BRK.B/FUTU 走真实代码路径）。

## Assumptions tested

1. `_sina_query_code()` 的 `.`→`$` 转换覆盖所有入口（单只 us.py、批量 us_batch.py、parse 的 wanted 映射），无遗漏路径。
2. 三个新测试断言的是关键行为本身，不是形同虚设（fake 响应与断言耦合会导致错误实现也通过）。
3. 修复未引入新回归（查询串编码、wanted 键碰撞、大小写回显）。
4. 文档残留已清零。

## Findings

### PR-01-已修复-[高]-带点美股代码（BRK.B）在新浪路径下无法解析
- **修复验证**:
  - `src/pricing/providers/sina_us.py:37-39` 新增 `_sina_query_code()`（`code.lower().replace(".", "$")`），并同时用于查询串拼接（`:123`）和 `parse_sina_us_quotes` 的 `wanted` 映射（`:54`）——上一轮指出的两个改造点都落实。
  - 入口覆盖确认：单只路径 `us.py:104` 与批量路径 `us_batch.py:68` 都汇聚到 `fetch_sina_us_quotes()`，无绕过 `_sina_query_code` 的第三入口（全仓 grep `sina_us` 仅这三处调用 + 测试）。
  - 新测试 `test_sina_us_dotted_symbol_maps_to_dollar_query_code`（tests/test_pricing_providers.py）断言 URL 含 `gb_brk$b` 且响应 var `hq_str_gb_brk$b` 能解析回原始代码 `BRK.B`——断言的是关键行为，非摆设。
  - 本机实盘端到端验证（真实代码路径，非 curl）：`fetch_sina_us_quotes(fetcher, ["BRK.B","FUTU"])` 返回 `BRK.B 伯克希尔B 489.65 prev_close=491.25 source=sina_us`、`FUTU 富途控股 103.66`。`$` 在 query 中无需编码，requests 原样发送，已证实可用。
  - `decisions/2026-07-22-us-pricing-sina.md` 已补充 `$` 转换说明。
- **残留核对**: wanted 键碰撞（`BRK.B` vs 字面含 `$` 代码）现实中不存在；新浪回显 var 名与查询串逐字符一致（小写），大小写匹配无问题。

### PR-02-已修复-[中]-us_batch 核心新行为无测试覆盖
- **修复验证**:
  - `test_us_batch_merges_finnhub_failures_into_one_sina_request`：patch finnhub key + `fetch_finnhub` 全抛异常，断言**请求次数==1**、URL 含 `gb_futu,gb_baba`、两代码均以 `source=sina_us` 命中。若实现退化为逐代码请求或 leftover 拼错，断言会失败——真实锁定。
  - `test_us_batch_break_path_merges_skipped_codes_into_sina_request`：5 只代码 Finnhub 全失败，触发 `consecutive_failures>=3` break，断言第 4、5 只（skipped 切片）也在唯一一次新浪请求的 URL 中且全部命中。若 `codes[index+1:]` 切片写错或未 extend leftover，`result["PDD"]` 直接 KeyError——覆盖到位。
  - 小遗漏（不构成发现）：break 路径未显式断言 Finnhub 调用次数==3（即第 4、5 只未再发 Finnhub 请求）；但 URL 包含全部 5 个代码已隐含该行为，可不再补。
- **验证证据**: `pytest tests/test_pricing_providers.py` 通过；全量 687 passed。

### PR-03-已修复-[低]-`docs/architecture.mmd` 残留 Yahoo Chart
- **修复验证**: `docs/architecture.mmd:60` 已改为 `Tencent / Finnhub / Sina US / East Money / FX`。全仓 grep `yahoo|Yahoo`（src/scripts/docs 根文档/SKILL/README/config.example）仅剩 `sina_us.py:3` 注释中的历史说明（解释为何替换，属合理保留）及 decisions/CHANGELOG/reviews 历史记录。

## 新发现

无。对抗式复核了修复引入面：`_sina_query_code` 无双胞胎实现、无遗漏调用点；`$` 字符 URL 安全性已实盘验证；wanted 映射键与响应回显一致；三个新测试的 fake 与断言无耦合漏洞（请求计数、URL 内容、结果分键均独立断言）。

## Open Questions

- 无。

## Residual risks and tracking

- **过期缓存兜底失效（`max_stale_after_expiry_sec=0`）**：维持上一轮结论——8 只实际持仓（含 BRK.B）均已实盘确认可被新浪覆盖，该 bug 不阻断本次事故闭环，但仍需作为独立工作项跟踪（新浪一旦不可达，"过期缓存"层完全失效，事故原样复现）。
- **服务器侧验证仍是闭环最后一环**：本机实盘 smoke 通过不等于广州阿里云主机可达性已复验；部署后需按计划验证步骤 3-4 在服务器跑 8 只持仓 smoke 并观察下一次定时任务 journal。部署本身按既定流程，不在本工作单元。
- 新浪单请求代码数上限未实测（当前 8 只持仓远低于此），若未来持仓大幅扩张需留意。

## Lens conclusions

- **Architecture boundary**: 符号映射收敛为 `_sina_query_code()` 单一实现、同时服务查询与解析两侧，符合上一轮"统一契约"要求。
- **Best practice**: 决策文件同步记录 `$` 转换及出处（plan review PR-01），知识沉淀完整。
- **Optimal solution**: 修复面最小（一个纯函数 + 两处调用 + 三个测试），无过度设计。
- **Overengineering**: 无。
- **Overcoupling**: 无新增。

## Final conclusion

`pass`。PR-01/02/03 均修复且经代码级、测试级、实盘级三重验证；未发现修复引入的新问题。剩余事项仅为范围外的过期缓存 bug 跟踪与部署后服务器验证，不阻塞合并。
