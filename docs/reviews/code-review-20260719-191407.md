# Code Review — repo-review-27 S6

## Scope

- Mode: current changes
- Branch or PR: `gateflow/fix-repo-review-27`
- Base: `b676543`
- Output file: `docs/reviews/code-review-20260719-191407.md`
- Included scope: Feishu token/data timeouts, batch create/update/delete response validation, structured partial-write evidence, holdings/cash-flow/NAV/snapshot repository count/cache transitions, delete semantics, documented typed schema parsing, live field pagination, and strict comparison.
- Excluded scope: approved S7 service fallback/ASGI findings, release/deployment/live Feishu mutation, automatic compensation for already confirmed remote chunks, and undocumented future Feishu types.
- Parallel review coverage: 无。

## Change Intent and Contract Trace

- `FeishuClient.__init__ -> _get_tenant_token/_request`: configured positive connect/read tuple is the default hard upper bound; caller timeouts are clamped to the smaller value.
- `batch_create_records/batch_update_records/batch_delete_records`: every chunk must return the requested cardinality and usable IDs before it is appended to the confirmed prefix; later failures raise `FeishuBatchWriteError` with only prior complete chunks.
- `HoldingsRepository/CashFlowRepository/NavHistoryRepository/SnapshotsRepository`: count, record-ID, cache update, and cache invalidation occur only after the client returns a complete validated result. Delete exceptions propagate before cache removal.
- `scripts/migrate_schema.py`: documented field names and accepted type families are parsed once, live pages are exhausted through `has_more/page_token`, and strict mode blocks core required name/type drift.

## Findings

未发现实质性问题。

## Adversarial Failure Pass

- Missing/empty/short/long batch `records`, missing IDs, mismatched update/delete IDs, duplicate update request IDs, and a second-chunk timeout all fail without reporting the requested count as confirmed.
- `FieldNameNotFound` compatibility fallback is allowed only before any chunk is confirmed; partial confirmed batches are not replayed.
- Token fetch and all session request paths receive explicit timeouts; a larger explicit caller timeout cannot bypass configured limits.
- Delete configuration, transport, HTTP, and Feishu API errors propagate; holding cache invalidation is after confirmed success.
- Schema pagination rejects `has_more` without a fresh token and strict mode rejects a required type mismatch discovered on a later page.

## Project Instructions Pass

- No production/live Feishu or portfolio data was read or mutated.
- No deployment, release, merge, PR-ready transition, reviewer request, dependency addition, schema migration, or unrelated cleanup was introduced.
- Changes stay within the approved S6 ownership plus the required Gateflow artifacts.

## Overcoupling and Semantic Ownership Drift

- HTTP timeout and protocol-shape validation remain owned by `FeishuClient`; repositories consume one normalized result contract rather than duplicating Feishu response parsing.
- Repository cache/count ownership remains local to each repository and occurs only after protocol confirmation.
- Schema type knowledge is isolated to the read-only schema checker and docs; runtime repositories do not gain schema-migration responsibilities.
- No new framework, transport abstraction, or generic workflow engine was added.

## Validation

- Required S6 focused suite -> `134 passed`.
- Full repository suite -> `650 passed`.
- `python3 -m compileall -q src scripts tests` -> passed.
- `git diff --check` -> passed.

## Open Questions

无。

## Residual Risk

- Future undocumented Feishu response/type changes are `covered by strict failure and later deployment canary`.
- A timeout after a remote write was accepted remains outcome-unknown; this slice exposes truthful structured evidence, and S7 owns preventing automatic service fallback replay.
- Confirmed prior chunks require caller/operator recovery; automatic compensation is outside the approved S6 scope and is `assigned to existing compensation/operator recovery paths`.

## Decision

- Gate result: `code review pass`.
- Accepted findings: `0`.
- Open questions: none.
- Residual risks: classified.
- Next gate: `accepted slice commit`.
