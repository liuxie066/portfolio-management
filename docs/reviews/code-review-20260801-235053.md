# Code Review

## Scope

- Mode: current changes
- Branch or PR: `gateflow/feishu-bitable-contract-repair`
- Base: `e45a30ec8934`
- Output file: `docs/reviews/code-review-20260801-235053.md`
- Included scope: S1 after final scoped re-review, plus full-suite integration evidence
- Excluded scope: unrelated review artifact, S2-S12 implementation, live Feishu operations
- Parallel review coverage: 无；本轮由全量测试收集路径反查 migrate_schema public import compatibility

## Findings

### DR-S1-RR2-01-未修复-中-删除 parse_docs_schema 破坏已有导入契约并阻断全量测试收集
- **入口/函数**: 导入 `scripts.migrate_schema.parse_docs_schema`
- **文件(行号)**: `scripts/migrate_schema.py`; `tests/test_entrypoint_consolidation.py:7,120`
- **输入场景**: 运行全量 pytest，或任何现有工具继续导入该 helper。
- **实际分支**: 模块已完全删除 `parse_docs_schema`；Python 在测试收集期抛 `ImportError`，测试体尚未执行。
- **预期行为**: docs 不再是运行时真源，但已有 import surface 应以 registry-backed compatibility projection 收口，或同一 slice 更新所有调用方；不能留下不可导入状态。
- **实际行为**: S1 scoped suite通过，但 repository full suite在 collection 阶段失败。
- **直接证据**: `PYTHONPYCACHEPREFIX=/tmp/pm_s1_full python3.12 -m pytest -q -p no:cacheprovider` 返回 collection error：`cannot import name 'parse_docs_schema'`。仓库唯一调用方位于 `tests/test_entrypoint_consolidation.py`。
- **影响**: 当前 slice commit 会让 CI 无法进入测试执行；也会直接破坏可能依赖该脚本 helper 的本地工具。
- **建议改法和验证点**: 在 `migrate_schema.py` 保留明确标记为 compatibility 的 `parse_docs_schema()`，但让它只投影 `TABLE_CONTRACTS`、完全不读/解析 markdown；增加测试证明传入任意文档内容不会改变结果。后续把旧测试对 skipped optional 的过时语义更新为新 contract。
- **修复风险（低/中/高）**: 低
- **严重程度（低/中/高/严重）**: 中

## Open Questions

- 无。

## Residual Risk

- `tests/test_entrypoint_consolidation.py` 还固化了旧的 non-strict field metadata 和 `optional ok=True` 语义；该文件在 accepted plan 中属于 S8 allowed scope，需在 S8 改为 exact metadata 与 `skipped_unconfigured/ok=None`。
