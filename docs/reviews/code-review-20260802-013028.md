# Code Re-Review

## Scope

- Mode: current changes re-review
- Branch: `codex/feishu-dual-app-credentials`
- Base: accepted S1 commit `078ec95`
- Prior review: `docs/reviews/code-review-20260802-012915.md`
- Fix artifact: `docs/gateflow/feishu-dual-app-credentials/s2-fix.md`
- Output file: `docs/reviews/code-review-20260802-013028.md`
- Included scope: S2 fixes plus canonical Bitable/Conversation consumer routing,
  event status, adapters, request-map invariants, and focused tests
- Excluded scope: S3-S4, live Feishu requests, release, deployment, production
  credential provisioning, and plaintext cleanup
- Parallel review coverage: 无；slice is a narrow role-routing change

## Findings

未发现新的实质性问题。

## Prior finding status

### S2-CR-01 — 已修复

The holdings compatibility status command now resolves canonical Bitable App ID
and App Secret independently from the holdings table target. Credential issues
and target errors have separate output fields. The new regression proves that a
missing target reports invalid target status while both identity fields remain
configured and no SDK client is constructed.

## Evidence

- Focused S2 suite: `183 passed`.
- `git diff --check`: passed.
- Legacy production-consumer search for `feishu.app_*` and
  `feishu.receipt.*`: no matches outside the compatibility resolver.
- Existing adapter request-map tests still assert `file_token` and
  `file_type=bitable` without outbound event type.

## Open Questions

- 无。

## Residual Risk

- Runtime systemd credential delivery and exact unit grants remain S3.
- No live subscription or listener connection was exercised in this source-only
  slice.

## Conclusion

`pass`

All accepted S2 findings are fixed and the slice may be committed.
