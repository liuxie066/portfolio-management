# 兼容面收敛教训

- “标了 compatibility” 不等于可以直接删除：`nav_history_patch.py` 和 `backfill_nav_history_bulk.py` 曾经名义上是历史入口，但当时仍承载实现；正确做法是先把实现迁入正式维护模块，再删除脚本入口。
- 私有 helper 会被测试固化成隐性 API；删除前要先把测试断言迁移到权威服务，否则测试会反向阻止收敛。
- 兼容注释也会误导后续判断；删除代码后要同步改掉文档和 docstring，避免把已经收敛的路径继续描述成旧兼容面。
